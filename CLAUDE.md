# CLAUDE.md 최종본 (v3.2, 2026-06-14 정비)
# 기존 #1~#41 통합정리 + 파일 생성·구글 산출물(#42~#43) + 도구 가용성(#44~#47) + 공유드라이브 이동금지(#16-B)
# 규칙 수: 총 48개 (#1~#47 + #16-B, 결번 없음)

---

모든 답변과 사고과정은 한국어로 진행

## 1. 소스 및 근거 규칙 (#1~#9)

적용 범위: 사용자가 소스 자료를 제공한 모든 법학 학습·LEET·발제 작업

#1 Source Grounding: 모든 단정은 사용자 제공 소스에만 근거한다. 외부 지식·추측으로 답변하지 않는다.

#2 Not-in-Source + Uncertainty: 소스에서 확인할 수 없는 단정은 출력하지 않는다. "소스에서 확인할 수 없습니다" 또는 "자료 부족—보류"로 대체한다. 불확실한 경우에도 동일하게 적용한다. (구 #12 통합)

#3 Evidence Mandatory: 법리/사실 단정 시 근거를 명시한다 (발췌 "……" + 위치 《파일명》 p.X). 소크라틱 모드에서는 최종 답변 시에만 근거를 제공한다.

#4 No Fabricated Location: 위치를 추정하지 않는다. 불명확 시 "(확인 불가—위치 식별자 부족)".

#5 Original-Text Priority: 조문/판례 "원문"을 우선한다. 원문이 없으면 "(원문 미포함)" 명시.

#6 Integrity: 조문번호, 판례명/선고일의 정확성을 최우선한다. 불명확 시 [불명확: 후보1/후보2].

#7 COT Disclosure: 내부 추론 과정은 기본적으로 노출하지 않는다. 사용자 요청 시 간결히 설명한다.

#8 Output Minimalism: 요청 형식만 출력한다. 장문을 지양한다. 모든 답변은 한국어 (코드/인용/전문용어는 원어 병기 가능).

#9 PDF Handling: Claude API의 네이티브 PDF 지원을 사용한다. pypdf 로컬 전처리는 RAG 청킹/인덱싱 용도로만 사용한다.

## 2. 출력·사고 제어 (#10~#11)

#10 Structured Thinking: 내부적으로 단계별 추론을 수행한다. effort 파라미터가 thinking 깊이를 제어한다. 일반 응답에서는 추론 과정을 숨긴다 (#7 준수).

#11 XML Structure (선택): 복잡 입력 시 `<context>`, `<task>` 등 태그를 사용한다.

## 3. 소스 검증 (#12~#13)

적용 범위: 법학 학습, LEET 문제 풀이

#12 소스 검색: Grep/Read로 소스 파일을 검색한다. 의미 검색이 빈약하면 키워드로 보완한다. 복잡 질의 시 하위 질의로 분해하여 순차 탐색한다.

#13 생성 후 검증: 생성 완료 후 법리/사실 단정을 소스와 대조한다. 소스 확인 안 되면 해당 단정을 삭제한다 (#2 적용). "~인 것 같다" 등 추측 표현도 사용하지 않는다. OCR 교정 결과도 검증 대상. 조문번호·사건번호는 korean-law-mcp로 자동 검증한다. OCR 파이프라인은 3단계 (노트북 2종은 워크스페이스 루트에 위치 — Colab 연동 경로이므로 이동 금지): ①Colab `ocr_extract_v2.ipynb`(marker-pdf 추출 → `sync/_ocr_extracted/`) → ②Colab `ocr_compare_v2.ipynb`(기존 md vs 새 추출 비교 → `corrections.jsonl` + `verification_targets.json`) → ③Claude Code(`haiku_ocr_correct.py` + `sonnet_review.py` + `apply_corrections.py` + korean-law-mcp 검증). Colab은 무료, Claude Code는 Max 구독 포함.

## 4. 인용 (#14)

적용 범위: 법학 학습, LEET 문제 풀이

#14 Prompt-Based Citation: 법리/논증 응답에 인라인 인용을 적용한다.
- 형식: [《문서명》 p.X | "정확한 발췌"]
- PDF: 페이지 번호, 텍스트: 청크 ID
- 검증 후 인용 불가 시 해당 단정을 삭제한다 (#2 적용)

## 5. 파일 작업 원칙 (#15~#16)

#15 Verify-Before-Act + Checklist:
- 파일 이동/이름변경 전 해당 파일 내용 또는 메타데이터를 확인한다
- 워크플로우/스킬 수정 전 현재 내용을 읽는다
- 폴더 구조 작업 전 현재 상태를 확인한다
- 파일 2개 이상 변경 시 변경 목록(파일명+1줄 요약+순서)을 사전에 작성한다
- 확인 없이 추측으로 작업하지 않는다
- OCR 교정 시 원본을 HTML 주석(<!-- original: ... -->)으로 보존한다.

#16 삭제 금지 원칙:
- 파일/폴더를 삭제하지 않는다. Remove-Item, del, rm, rmdir 등 삭제 명령을 사용하지 않는다
- 삭제 대신 `_trash/{YYYY-MM-DD}/` 하위 폴더로 이동한다 (표준: 워크스페이스 루트 `_trash/`로 단일화, 2026-06-11. 구 `5.기타/_trash/`는 레거시 보관 동결)
- 빈 폴더도 삭제하지 않는다. 정리 필요 시 사용자에게 먼저 확인한다
- 백업 파일은 sync 내부가 아닌 5.기타/ 하위에 저장한다
- 이 규칙은 항상 적용된다

#16-A 빈 파일 자동 정리 (2026-06-15 신설):
- 0바이트 파일 또는 공백·줄바꿈만 있는 빈 텍스트 파일을 발견하면 `_trash/{YYYY-MM-DD}/`로 이동을 권고한다
- 신규 생성 파일(작업 중 생긴 임시·실험 산출물 등)에 한해 자동 이동을 허용한다. 기존 파일은 사용자 확인 후 이동한다
- 시스템 설정 파일(`.env`·`.gitignore`·`.mcp.json`·`desktop.ini` 등)과 구글 워크스페이스 포인터(`.gdoc`·`.gsheet`·`.gslides`·`.gscript`)는 빈 것처럼 보여도 자동 이동에서 제외한다 (#43, #16-B 우선)
- 삭제가 아닌 이동이며 #16 삭제 금지 원칙을 준수한다

#16-B 공유드라이브 이동·삭제 금지 — 최우선 강제 (2026-06-14 신설):
- `0.공유드라이브/` 또는 Google 공유 드라이브 경로의 어떤 파일/폴더도 이동·이름변경·삭제하지 않는다
- 사유: Google 공유 드라이브는 스터디원 공동 소유 — 이동·삭제 시 타인 드라이브에 동기화되어 영향(의도치 않은 동기화·접근 권한 변경 위험)
- 읽기·복사는 허용한다. 그러나 쓰기·이동·이름변경·삭제는 사용자 명시 승인 후에만 수행한다
- 본 룰은 어떤 작업·자동화(폴더 정리·파이프라인·스킬 포함)에서도 우선한다. #16 삭제 금지보다 강한 제약이며, 공유드라이브에는 `_trash` 이동조차 적용하지 않는다

#16-C 파일 이동·이름변경 자동 로그 의무 (2026-06-15 신설):
- 워크스페이스 내 파일/폴더를 이동·이름변경·복사·`_trash` 이동할 때마다 반드시 `.agent/file_ops_log/master.{csv,jsonl,md}` 3개 형식에 1작업=1행으로 기록한다
- 기록 수단: `.agent/scripts/log_file_op.py`(또는 `.ps1`) 헬퍼 사용을 원칙으로 한다. `--execute` 모드는 이동 실행과 전후 sha256 무결성 검증·로그를 한 번에 처리한다. 다른 도구(`mv`·`Move-Item`)로 이동한 경우 직후 `--execute` 없이 헬퍼로 로그만 보충한다
- 표준 필드: `timestamp / operation / source_path / dest_path / size_bytes / sha256 / task_id / reason / verified` (기존 이동 매니페스트 형식과 정합)
- 로그 누락 시 해당 작업은 무효로 간주하고 즉시 보충 기록하거나 되돌린다
- 공유드라이브(`0.공유드라이브/`)에는 본 룰을 적용하지 않는다 — 애초에 이동·쓰기가 금지(#16-B)되므로 로그 대상이 아니다. 헬퍼도 해당 경로를 차단한다
- 과거 이동 기록은 `.agent/file_ops_log/_legacy_consolidated.csv` 에 통합 보존(2026-06-15, 87건). 트리거·사용법은 `.agent/skills/file_ops_log/SKILL.md` 참조

## 6. 워크플로우 자동 참조 (#17~#18)

#17 Context-Based Auto-Load:

| 맥락 패턴 | 워크플로우 |
|----------|----------|
| 파일 분류, 이름 변경 | file-classification (Skill) |
| 학습 질문, 개념 이해, 문제 풀이 | socratic.md |
| 노트 정리, 수업정리, 핵심정리 | lecture-notes.md |
| LEET, 추리논증, 언어이해 풀이 | leet-solve.md |
| 세션 시작, 새 대화 | sync/wiki/_index.md 자동 참조 (#38) |
| "메모리 정비", "lint" | lint.py 실행 (#41) |
| "메모리 컴파일" | flush.py + compile.py (#41) |
| "OCR 교정", "corrections" | haiku_ocr_correct.py + sonnet_review.py + apply_corrections.py |
| 단권화 책 전체 카드화, "PDF 처리 단권화", "책 1권 카드 만들어" | claude_code_package_v2/SKILL.md 워크플로우 A (Claude Code가 prompts/02-wiki→02-card 순차 적용) |
| Obsidian 위키화만, "PDF 위키화" | claude_code_package_v2 워크플로우 D (prompts/02-wiki_claude_위키화_v3.6.xml 적용) |
| 사례집 풀이 포함 자동 카드화, "06b" | claude_code_package_v2 워크플로우 B (prompts/06b_claude_사례집문제_extract_v3.6.xml 적용) |
| 사례집 1문제 정밀 카드화, "06a" | claude_code_package_v2 워크플로우 C (prompts/06a_claude_사례집문제_manual_v3.6.xml 적용) |
| 01번 OCR 단독, "마킹 추출" | claude_code_package_v2 (prompts/01_claude_마킹_구조_조문_추출_v3.6.md 적용) |
| "찌라시", "시험장 압축", 시험 직전 A4 정리 | claude_code_package_v2 (prompts/07_claude_찌라시_시험직전압축_v1.md 적용 — 쟁점 아티클·v4 카드 압축) |
| 쟁점 위키 아티클 생성, "쟁점 아티클" | claude_code_package_v2 (prompts/03-wiki-rollup_쟁점아티클_v1.md 적용 — 카드 roll-up) |
| 파일 이동·이름변경·복사·trash 이동, "이동", "이름변경", "move", "rename" | file_ops_log (Skill) — log_file_op.py 로 master.{csv,jsonl,md} 자동 기록 (#16-C) |

주의: "학습 질문"과 "노트 편집/보강/검토 작업"을 구분한다. 노트 편집 요청에 소크라틱 모드를 적용하지 않는다.
주의: 신규 패키지(claude_code_package_v2)는 책 → 카드 자동화 프롬프트 묶음이고, 소크라틱(socratic.md)은 학습 모드. 별개 시스템으로 병행 운영.
주의: 신규 패키지 v3.6 Phase 2 (2026-06-01) — 2모델 역할분담 운영 모드. 안티그래비티 앱(데스크톱 프로그램 — 탑재 모델은 교체 가능하므로 모델명을 룰에 고정하지 않는다) = 01 OCR + 02-wiki 위키화(대용량 원문 저비용 압축) / Claude Opus 4.8 = 02-card·06a·06b 카드화(위키 입력받아 빌드). 위키 10:1 압축으로 Opus 입력 토큰 최소화. (이전 Phase 1.2: Claude Code 직접 처리 모드 — legacy 스크립트·.env 보존). 원본 백업: 5.기타/패키지백업/2026-05-21/. 산출물 저장 위치(2026-06-11): 02-wiki 위키화 → sync/위키/ (구 outputs/02_wiki에서 이관), 07 찌라시 → sync/찌라시/, 02-card·01_ocr → outputs/ 유지.
주의: 카드 대량 생성·룰 변경 시 운영 원칙(2026-06-12) — ①신규/변경 룰은 **1청크 파일럿**으로 검증(앞/뒤/빈칸 직접 확인) → ②통과 시에만 **전체 병렬 재작성**(워크플로우, 파일럿 통과한 작업은 사례형·암기장 등 병렬 동시 가동). 파일럿 없이 전량 실행 금지.

#18 Auto-Load Behavior: 맥락 감지 → 워크플로우/스킬 파일을 읽은 후 적용한다. 슬래시 명령 → 즉시 해당 파일을 읽고 적용한다.

파일 존재 확인 완료 (2026-04-24, 2026-05-21 v3.6 Phase 1.2 갱신, 2026-06-01 v3.6 Phase 2 2모델 분담 갱신):
- [x] .agent/workflows/socratic.md
- [x] .agent/workflows/lecture-notes.md
- [x] .agent/workflows/leet-solve.md
- [x] .agent/skills/file-classification/SKILL.md
- [x] 프롬프트 등 개선/claude_code_package_v2/SKILL.md (v3.6 Phase 1.2 — Claude Code 운영 모드)
- [x] 프롬프트 등 개선/claude_code_package_v2/prompts/ — 총 14개 (claude 5종: 01·02-wiki·02-card·06a·06b / gemini 5종 / 공용 4종: 03-wiki-rollup·04-notebooklm·05-anki·07-찌라시) (2026-06-11 확인)
- [x] 5.기타/패키지백업/2026-05-21/claude_code_package_v2/ (원본 백업)

## 7. 진도 추적 (#19~#20)

#19 TOC-Based Progress: 교재 목차 경로 기준. 형식: {편}>{장}>{절}>{항목}

#20 Weak Problem Storage: .agent/state/learning.json → weak_points 저장. Spaced Repetition: .agent/state/srs_log.json에서 간격 관리 (SM-2).

## 8. 학습 행동 원칙 (#21~#25)

#21 Socratic First: 학습 질문(개념 이해, 문제 풀이) → /socratic 우선. 노트 편집/보강/검토 같은 작업 요청에는 적용하지 않는다.

#22 Label Restriction: "기출/빈출" 라벨은 소스에 표지가 있을 때만 사용하고 근거 2줄을 붙인다.

#23 Scoring Constraint: 배점표가 있으면 반영하고, 없으면 O/△/X만 사용한다.

#24 IRAC Block: 사례형 → 청구권/항변 블록, 근거 2줄을 포함한다.

#25 Source Priority: 사례형 교재 > 사례형 자료 > 선택형 자료 > 정리 자료

## 9. 모드 제어 (#26~#28)

#26 Stop Word: "종료" → 현재 흐름을 종료한다.

#27 Auto/Manual Toggle: "자동"=약점 즉시 제시, "수동"=메뉴 질문.

#28 Only-on-Request: 전사/발췌는 명시적 요청 시에만 수행한다.

## 10. 코딩 규정 (#29~#31)

#29 Rule Consistency: 코드는 #1~#16과 일관되게 작성한다.

#30 Output Compliance: 법학 학습 관련 스크립트 출력에는 근거 2줄을 준수한다. 시스템 유틸리티(OCR, reflow, lint 등) 출력에는 적용하지 않는다.

#31 Code Integrity: 기존 코드 수정 시 기능을 보존한다. 주요 변경 전 테스트한다. 음성 전사 시 .agent/skills/whisper-transcribe/scripts/transcribe.bat 로컬 우선 사용한다.

## 11. 노트 정리 형식 (#32~#35)

적용 범위: 민법·형법·헌법 등 모든 과목의 사례형 답안 및 정리노트

#32 청구-항변 구조: 사례형 청구-항변 구조는 2열 표(원고|피고)로 정리한다. 트리거: 사용자가 사례문제를 풀거나 청구-항변 구조를 요청할 때 적용한다. 형식: `| **원고 (甲)** | **피고 (乙)** |` + 행 단위로 청구→항변→재항변 주고받기, 빈 셀은 `—`. 3열 표, 볼드 블록, 줄글형은 사용하지 않는다.

#33 모답형/보강포인트 배치: 사례형 답안 연습(모답형), 보강 포인트, 교차검증 결과 등은 해당 쟁점 섹션 안에 배치한다. 부록 성격(암기 문장, 시험 직전 접근순서, 판례 총정리표 등)은 별도 유지한다.

#34 판례 문구 원칙: 판례의 태도/입장을 기술할 때는 교재·판례 원문에 확인된 표현만 사용한다. 전사본이나 요약에서 온 비공식 표현은 교재 원문 표현으로 대체한다. 판례 사건번호를 함께 표기한다. OCR 교정에서도 동일 적용. anchor(조문번호·사건번호·한자)는 LLM 교정 대상에서 제외한다.

#35 백링크 규칙: 노트 내에서 판례, 조문, 핵심 개념에 옵시디언 스타일 백링크를 사용한다.
- 판례: [[대판 YYYY.M.DD, YYdaXXXX]] 또는 [[헌재 YYYY.M.DD, YYYYhunmaXXXX]]
- 조문: [[§XXX]] (§10 이상만. §1~§9는 섹션번호 오탐 방지를 위해 제외)
- 핵심 개념: [[선의취득]], [[물권적청구권]] 등
- 표·각주·코드블록 안에서는 백링크를 걸지 않는다
- 적용 범위: 정리노트, 압축본, 교재 추출본, wiki 아티클

## 12. 교수님 표현 및 외국어 (#36~#37)

#36 교수님 고유 표현 태그: 각 교수님만의 독특한 표현·용어는 [전경운], [서보학], [강혜림] 등 태그로 별도 표시한다. 적용 범위: 전 과목 정리노트 및 압축본.

#37 외국어 금지: 노트 본문에 라틴어·독일어·한자 등 외국어 표현을 사용하지 않는다. 한국어 대역만 기재한다. 교재 인용 시에도 한국어로 번역하여 기재한다.

## 13. Wiki 메모리 시스템 (#38~#41)

적용 범위: 모든 세션, 모든 과목

#38 세션 시작 시 Wiki 참조: 세션 시작 시 sync/wiki/_index.md를 읽고, 사용자의 질문과 관련된 아티클이 있으면 해당 아티클을 먼저 읽은 후 응답한다.
- _index.md가 없으면 MEMORY.md만 참조 (기존 동작 유지)
- _index.md는 25KB 이하로 유지한다
- 개별 wiki 아티클은 필요할 때만 읽는다 (전부 로드하지 않는다)
- wiki는 이전 세션의 작업 기록이므로 "사실"이 아닌 "참고"로 취급한다
- 교재 소스(#1)보다 wiki가 우선하지 않는다

#39 세션 종료 시 자동 기록: 세션에서 다음에 해당하는 내용이 있었으면 .auto-memory/session_logs/에 기록한다:
- 노트 수정/보강 작업의 결과 요약
- 새로 확인된 판례·학설·교수님 입장
- 사용자가 지적한 오류와 수정 내용
- 작업 중 발견한 교훈
- 기록 형식: session_{YYYY-MM-DD}_{HHMM}.md

#40 Wiki 아티클 원칙:
- wiki 아티클은 교재 소스(#1~#5)를 대체하지 않는다
- wiki에서 확인한 내용도 교재 원문과 대조 후 사용한다
- wiki 아티클 내 판례·조문은 반드시 소스 재확인한다
- wiki 아티클에 [[백링크]]를 사용한다 (#35 준수)
- wiki 아티클은 sync/wiki/ 폴더에만 저장한다

#41 정기 정비:
- "메모리 정비" 또는 "lint" 요청 시 → .agent/scripts/lint.py 실행
- "메모리 컴파일" 요청 시 → flush.py + compile.py 실행
- 오래된 아티클은 삭제하지 않고 _trash로 이동 (#16 준수)
- flush 완료된 세션 로그는 5.기타/세션로그_아카이브/로 이동 (삭제 아님)
- 증분 컴파일이 기본이고 --rebuild는 필요시에만 사용
- schedule 스킬로 주기적 정비를 자동화할 수 있다 (예: 매주 일요일 컴파일+lint)

## 14. 파일 생성 위치·구글 산출물 (#42~#43, 2026-06-11 신설)

#42 파일 생성 위치: Claude가 새 파일을 만들 때 워크스페이스 루트에 생성하지 않는다. 유형별 표준 위치:
- 일회성/실험 스크립트: `.agent/scripts/` (일회성은 접두사 `_`)
- 상태·데이터(json): `.agent/state/`
- 운영 메모·검토안·보고서(.md): `sync/_meta/`
- 비노트 산출 문서(docx·xlsx·pdf 등): `5.기타/문서/`
- 백업: `5.기타/` 하위(#16) / 학습 자료 산출물: classification-rules_v2.md 분류 Flow 위치
- 작업 중 임시 파일은 완료 즉시 `_trash/{YYYY-MM-DD}/`로 이동한다
- 예외: 사용자가 위치를 명시한 경우

#43 구글 워크스페이스 산출물: `.gdoc`/`.gsheet`/`.gslides`/`.gscript`는 클라우드 포인터 파일로 로컬에서 직접 읽을 수 없다(Read·shell 모두 차단됨). 내용 열람은 Google Drive MCP(search_files → read_file_content)로 한다. 루트에 쌓인 포인터는 `5.기타/구글문서/`로 집결한다(이동 시 Drive 웹 폴더도 함께 변경됨 — 집결은 사용자 승인 정책, 2026-06-11). 예외: 밀메뉴 관련 파일·Gemini Gems·Colab Notebooks는 루트 보관, Opal은 폐기됨.

## 15. 도구 가용성·충돌 방지 (#44~#47, 2026-06-11 신설)

#44 PDF 추출 폴백 체인:
PDF 텍스트가 필요하면 **추출 전에 기존 마크다운 추출본부터 확인한다(#46)**. 이미 md가 있으면 PDF를 다시 추출하지 않는다. 새 추출이 필요할 때만 다음 순서로 시도하고, 실패 시 자동으로 다음 단계 후 결과 보고:
1. Read 도구 (작은 PDF, 1MB 미만)
2. **pdfplumber (Python)** — 한글 추출 가장 안정적 (권장)
3. pdftotext → pdftotext -raw (빠르나 한자·기호 일부 깨짐)
4. pypdf
5. 사용자에게 수동 변환 요청

#45 동시 작업 충돌 방지:
다른 도구(Antigravity, Cursor 등)와 같은 파일을 동시 편집할 가능성이 있으면:
- 작업 전 파일 크기·첫 줄 확인 — 외부 도구 흔적 발견 시 즉시 보고
- 충돌 위험 시 별도 파일명 사용 (`_v2`, `_claude`, `_정리`)
- Edit 전 항상 Read로 현재 상태 검증

#46 기존 마크다운 추출본 우선 (PDF 재추출 금지):
새 자료 추출 전 다음을 반드시 먼저 검색 (전체 맵: sync/_index_master.md):
- outputs/01_ocr_llamaparse/ — 교재 전권 OCR 마크다운 (1차 소스)
- outputs/02_cards/ — 카드화 완료분 (주제:: 태그로 grep 가능)
- sync/wiki/쟁점/ — 쟁점 아티클 (요건·판례 압축본)
- 5.기타/_백업/sync이관_2026-06-11/ (구 sync/_백업 — 교재원문_문서_백업_2026-05-22 포함), 5.기타/교재원문_백업/sync이관_2026-06-11/ (구 sync/1-1_*/원문_추출본), .agent/temp_toc/, .agent/temp_pdf_extract/
이미 있으면 새 추출하지 않는다. 없을 때만 #44 체인 실행.

#47 산출물 동기화 (조건부):
주요 산출 노트 작성 후 wiki 진입점(sync/wiki/_index.md)에 링크 추가(해당 토픽 존재 시), 세션 기록은 #39와 통합. 찌라시·카드 후보 추출은 #17 트리거(07·02-card 프롬프트)로 실행.
