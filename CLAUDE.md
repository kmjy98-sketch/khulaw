# CLAUDE.md 최종본 (v3.2, 2026-06-14 정비; 2026-06-21 위키경로 정본화·룰 정합 갱신)
# 기존 #1~#41 통합정리 + 파일 생성·구글 산출물(#42~#43) + 도구 가용성(#44~#47) + 공유드라이브 이동금지(#16-B)
# 규칙 수: #1~#52 + #16-A/B/C/D + #45-B/C/D (2026-07-01 — #52 모델티어링·채점관3단·복습3루프·자가채점 / #45-D 동시실행상한(대량 fan-out 금지, 소수·순차 Agent 기본) / 2026-06-28 — #51 카드모델·사례층(위키3층)·과목풀네임 / 2026-06-26 #50 위키닫힌루프·2층분리·검토게이트·도서관정본)

---

모든 답변과 사고과정은 한국어로 진행

## 0. 룰 모듈 로드 의무 (커널/모듈 구조, 2026-07-02)

본 파일은 **커널**(안전·불변 규칙 + 라우팅)만 담는다. 절차 상세는 `.agent/rules/` 모듈에 있으며, **아래 작업을 시작하기 전 대응 모듈을 Read하지 않았으면 그 작업을 진행하지 않는다**(요지만으로 진행 금지):

| 작업 유형 | 필독 모듈 |
|----------|----------|
| 파일 생성·이동·정리·PDF 분할·추출 | `.agent/rules/파일작업.md` (#16-D·#42상세·#43·#44·#46) |
| 서브에이전트·Workflow fan-out·대량 배치·검증 파이프라인 | `.agent/rules/에이전트운영.md` (#45-B/C/D 전문) |
| 위키화·쟁점추출·사례노트·카드 생성 | `.agent/rules/위키카드.md` (#50·#51 전문) |
| 드릴·채점·SRS·복습 구성 | `.agent/rules/채점복습.md` (#52 전문·#20 상세) |
| 사례형 답안·정리노트·아티클 작성 | `.agent/rules/노트형식.md` (#32~#37 전문) |
| 메모리 정비·세션 기록·자산 은퇴 | `.agent/rules/메모리정비.md` (#38~#41·#49 전문) |
| 웹·문헌·판례 조사 보고서 작성 | `.agent/rules/조사보고서.md` (#53 — 산문 기본·표≤3·출처 인라인·원시데이터 볼트이관·사실관계도·검증표기. #8 최소주의는 채팅 응답 한정) |

모듈도 룰 정본의 일부다(번호 체계 동일). 모듈 수정 시 커널 요지와의 정합을 함께 확인한다(#45-C 자기정합).

## 1. 소스 및 근거 규칙 (#1~#9)

적용 범위: 사용자가 소스 자료를 제공한 모든 법학 학습·LEET·발제 작업

#1 Source Grounding: 모든 단정은 사용자 제공 소스에만 근거한다. 외부 지식·추측으로 답변하지 않는다.

#2 Not-in-Source + Uncertainty: 소스에서 확인할 수 없는 단정은 출력하지 않는다. "소스에서 확인할 수 없습니다" 또는 "자료 부족—보류"로 대체한다. 불확실한 경우에도 동일하게 적용한다. (구 #12 통합)

#3 Evidence Mandatory: 법리/사실 단정 시 근거를 명시한다 (발췌 "……" + 위치 《파일명》 p.X). 소크라틱 모드에서는 최종 답변 시에만 근거를 제공한다.

#4 No Fabricated Location: 위치를 추정하지 않는다. 불명확 시 "(확인 불가—위치 식별자 부족)".

#5 Original-Text Priority: 조문/판례 "원문"을 우선한다. 원문이 없으면 "(원문 미포함)" 명시.

#6 Integrity: 조문번호, 판례명/선고일의 정확성을 최우선한다. 불명확 시 [불명확: 후보1/후보2].

#7 COT Non-Disclosure: 내부 추론 과정은 기본적으로 노출하지 않는다. 사용자 요청 시 간결히 설명한다.

#8 Output Minimalism: 요청 형식만 출력한다. 장문을 지양한다. 모든 답변은 한국어 (코드/인용/전문용어는 원어 병기 가능).

#9 PDF Handling: Claude API의 네이티브 PDF 지원을 사용한다. pypdf 로컬 전처리는 RAG 청킹/인덱싱 용도로만 사용한다.

## 2. 출력·사고 제어 (#10~#11)

#10 Structured Thinking: 내부적으로 단계별 추론을 수행한다. effort 파라미터가 thinking 깊이를 제어한다. 일반 응답에서는 추론 과정을 숨긴다 (#7 준수).

#11 XML Structure (선택): 복잡 입력 시 `<context>`, `<task>` 등 태그를 사용한다.

## 3. 소스 검증 (#12~#13)

적용 범위: 법학 학습, LEET 문제 풀이

#12 소스 검색: Grep/Read로 소스 파일을 검색한다. 의미 검색이 빈약하면 키워드로 보완한다. 복잡 질의 시 하위 질의로 분해하여 순차 탐색한다.

#13 생성 후 검증: 생성 완료 후 법리/사실 단정을 소스와 대조한다. 소스 확인 안 되면 해당 단정을 삭제한다 (#2 적용). "~인 것 같다" 등 추측 표현도 사용하지 않는다. OCR 교정 결과도 검증 대상. 조문번호·사건번호는 `.agent/lib/law_api.py`(법제처 직접 API — `verify-text`로 산출물 일괄대조, `verify-article`·`verify-case` 개별)로 자동검증한다. (korean-law MCP는 #49 은퇴 — law_api.py 단독. 인터랙티브 조회도 `law_api.py search-law/law-detail/search-prec`. **조문번호를 모를 때는 `law-toc "법령명" [키워드]`로 조문 목차를 먼저 조회해 관련 조문을 찾는다** — 조문번호 추측 금지. law-toc 결과의 **'정의·총칙(우선참고)'(§1 목적·§2 정의·정의어 목록)를 키워드 매칭보다 먼저 확인** — 특히 특별법은 정의 조문이 용어 의미를 규정하므로 필수(기본법은 불요 시 생략 가능).) OCR 파이프라인: 신규 추출=**LlamaParse 로컬**(→ `outputs/01_ocr_llamaparse/`) → 교정=Claude Code(`haiku_ocr_correct.py` + `sonnet_review.py` + `apply_corrections.py` + law_api 검증). (전환 경위 → `9.작업중/클로드/CLAUDE_변경이력.md`)

## 4. 인용 (#14)

적용 범위: 법학 학습, LEET 문제 풀이

#14 Prompt-Based Citation: 법리/논증 응답에 인라인 인용을 적용한다.
- 형식: [《문서명》 p.X | "정확한 발췌"]
- PDF: 페이지 번호, 텍스트: 청크 ID
- 검증 후 인용 불가 시 해당 단정을 삭제한다 (#2 적용)

## 5. 파일 작업 원칙 (#15~#16)

#15 Verify-Before-Act + Checklist:
- 파일 이동/이름변경 전 해당 파일 내용 또는 메타데이터를 확인한다
- 워크플로우/스킬 수정 전 현재 내용을 읽는다
- 폴더 구조 작업 전 현재 상태를 확인한다
- 파일 2개 이상 변경 시 변경 목록(파일명+1줄 요약+순서)을 사전에 작성한다
- 확인 없이 추측으로 작업하지 않는다
- OCR 교정 시 원본을 HTML 주석(<!-- original: ... -->)으로 보존한다.
- 블로커 처리(2026-06-26 신설): 도구·권한 차단(MCP 접근거부·경로제한·PDF변환 불가 등)으로 작업이 막히면 **'못 한다/차단됨'으로 끝내지 않는다**. (a)원인과 필요한 권한·대안을 사용자에게 알리고 (b)**승인 또는 우회방법을 질문한 뒤 진행**한다. 블로커를 무단 방치하거나 임의 우회로 강행하지 않는다.

#16 삭제 금지 원칙:
- 파일/폴더를 삭제하지 않는다. Remove-Item, del, rm, rmdir 등 삭제 명령을 사용하지 않는다
- 삭제 대신 `_trash/{YYYY-MM-DD}/` 하위 폴더로 이동한다 (표준: 워크스페이스 루트 `_trash/`로 단일화, 2026-06-11. 구 `5.기타/_trash/`는 레거시 보관 동결). 단, `0.공유드라이브/`는 _trash 이동조차 금지 — #16-B 우선
- 빈 폴더도 삭제하지 않는다. 정리 필요 시 사용자에게 먼저 확인한다
- 백업 파일은 sync 내부가 아닌 5.기타/ 하위에 저장한다
- 이 규칙은 항상 적용된다

#16-A 빈 파일 자동 정리 (2026-06-15 신설):
- 0바이트 파일 또는 공백·줄바꿈만 있는 빈 텍스트 파일을 발견하면 `_trash/{YYYY-MM-DD}/`로 이동을 권고한다
- 신규 생성 파일(작업 중 생긴 임시·실험 산출물 등)에 한해 자동 이동을 허용한다. 기존 파일은 사용자 확인 후 이동한다
- 시스템 설정 파일(`.env`·`.gitignore`·`.mcp.json`·`desktop.ini` 등)과 구글 워크스페이스 포인터(`.gdoc`·`.gsheet`·`.gslides`·`.gscript`)는 빈 것처럼 보여도 자동 이동에서 제외한다 (#43, #16-B 우선)
- 삭제가 아닌 이동이며 #16 삭제 금지 원칙을 준수한다

#16-B 공유드라이브 이동·삭제 금지 — 최우선 강제 (2026-06-14 신설):
- `0.공유드라이브/` 또는 Google 공유 드라이브 경로의 어떤 파일/폴더도 이동·이름변경·삭제하지 않는다
- 사유: Google 공유 드라이브는 스터디원 공동 소유 — 이동·삭제 시 타인 드라이브에 동기화되어 영향(의도치 않은 동기화·접근 권한 변경 위험)
- 읽기·복사는 허용한다. 그러나 쓰기·이동·이름변경·삭제는 사용자 명시 승인 후에만 수행한다
- 본 룰은 어떤 작업·자동화(폴더 정리·파이프라인·스킬 포함)에서도 우선한다. #16 삭제 금지보다 강한 제약이며, 공유드라이브에는 `_trash` 이동조차 적용하지 않는다

#16-C 파일 이동·이름변경 자동 로그 의무 (2026-06-15 신설):
- 워크스페이스 내 파일/폴더를 이동·이름변경·복사·`_trash` 이동할 때마다 반드시 `.agent/file_ops_log/master.{csv,jsonl,md}` 3개 형식에 1작업=1행으로 기록한다
- 기록 수단: `.agent/scripts/log_file_op.py`(또는 `.ps1`) 헬퍼 사용을 원칙으로 한다. `--execute` 모드는 이동 실행과 전후 sha256 무결성 검증·로그를 한 번에 처리한다. 다른 도구(`mv`·`Move-Item`)로 이동한 경우 직후 `--execute` 없이 헬퍼로 로그만 보충한다
- 표준 필드: `timestamp / operation / source_path / dest_path / size_bytes / sha256 / task_id / reason / verified` (기존 이동 매니페스트 형식과 정합)
- 로그 누락 시 해당 작업은 무효로 간주하고 즉시 보충 기록하거나 되돌린다
- 공유드라이브(`0.공유드라이브/`)에는 본 룰을 적용하지 않는다 — 애초에 이동·쓰기가 금지(#16-B)되므로 로그 대상이 아니다. 헬퍼도 해당 경로를 차단한다
- 과거 이동 기록은 `.agent/file_ops_log/_legacy_consolidated.csv` 에 통합 보존(2026-06-15, 87건). 트리거·사용법은 `.agent/skills/file_ops_log/SKILL.md` 참조

#16-D PDF 분할·추출(요지): 목차/대단원/색인 3분할 · 물리페이지 검증 · **pypdf 등 파이썬 금지(PDF플러그인·LlamaParse만)** · **폴더 임의 생성 금지**(통권=10.도서관, 분할=과목 봉투) · 완전성 게이트(목차 전 페이지 커버) · 이동 시 #16-C 로그. **상세 절차 필독 → `.agent/rules/파일작업.md`**

## 6. 워크플로우 자동 참조 (#17~#18)

#17 Context-Based Auto-Load:

| 맥락 패턴 | 워크플로우 |
|----------|----------|
| 작업 단일 진입점, "이거 처리해줘", "알아서", "뭐부터", "/dispatch" (대상 애매) | dispatch (Skill) — 분류→LLM/코드 자동분배(#45-B)→보고. 기존 트리거 정확매칭은 해당 스킬 우선 |
| 파일 분류, 이름 변경 | file-classification (Skill) |
| 학습 질문, 개념 이해, 문제 풀이 | socratic.md |
| 노트 정리·핵심정리(신규 생성) | study-notes (Skill, 실행) · lecture-notes.md (참조 워크플로) |
| 노트 보완·검토, "판례 추가", "요건 채워", "노트 검토" | law-note-supplement (Skill) — 기존 노트 보강(신규생성 아님) |
| LEET, 추리논증, 언어이해 풀이 | leet-solve.md |
| 세션 시작, 새 대화 | sync/위키/_MOC.md + sync/위키/{과목}/ 쟁점 위키 참조 (#50, 구 원문/_index 폐기) |
| "메모리 정비", "lint" | lint.py 실행 (#41) |
| "메모리 컴파일" | flush.py + compile.py (#41) |
| "OCR 교정", "corrections" | haiku_ocr_correct.py + sonnet_review.py + apply_corrections.py |
| 단권화 책 전체 카드화, "PDF 처리 단권화", "책 1권 카드 만들어" | claude_code_package_v2/SKILL.md 워크플로우 A (Claude Code가 prompts/02-wiki→02-card 순차 적용) |
| Obsidian 위키화만, "PDF 위키화" | claude_code_package_v2 워크플로우 D (prompts/02-wiki_claude_위키화_v3.6.xml 적용) |
| 사례집 풀이 포함 자동 카드화, "06b" | claude_code_package_v2 워크플로우 B (prompts/06b_claude_사례집문제_extract_v3.6.xml 적용) |
| 사례집 1문제 정밀 카드화, "06a" | claude_code_package_v2 워크플로우 C (prompts/06a_claude_사례집문제_manual_v3.6.xml 적용) |
| 01번 OCR 단독, "마킹 추출" | claude_code_package_v2 (prompts/01_claude_마킹_구조_조문_추출_v3.6.md 적용) |
| "찌라시", "시험장 압축", 시험 직전 A4 정리 | claude_code_package_v2 (prompts/07_claude_찌라시_시험직전압축_v1.md 적용 — 쟁점 아티클·v4 카드 압축) |
| 쟁점 위키 아티클 생성, "쟁점 아티클" | claude_code_package_v2 (prompts/03-wiki-rollup_쟁점아티클_v1.md 적용 — 카드 roll-up) |
| 파일 이동·이름변경·복사·trash 이동, "이동", "이름변경", "move", "rename" | file_ops_log (Skill) — log_file_op.py 로 master.{csv,jsonl,md} 자동 기록 (#16-C) |
| 일일 문제풀이 루틴, "오늘 공부", "오늘 문제", "오늘 드릴", "오늘 복습", "/drill", "문제 내줘" | daily-drill (Skill) — 진도 기반 객관식 출제·즉시채점(정답키 선확정→진리값 대조) + 사례형 파일채점(case-answer-review) + 약점 SRS 연동 |
| 사례답안 채점, "올렸어", "업로드함", "사례 채점" | daily-drill (Skill) Phase 2 — 6.진도관리/사례답안/ 최신 파일 → case-answer-review |
| 주간 리뷰·회고, "주간리뷰", "이번주 정리", "이번주 어땠어", 주간 학습 마감 | weekly-review (Skill) — 주간 집계+약점 종합+시험일 역산 페이싱(deadline-aware)+다음주 과목별 3토픽 |
| 어법·문장 다듬기, "문장 다듬어", "어법 점검", 법률문장론 | 글다듬기 (Skill) — 임승순 법률문장론 9원리 어법·구조 점검(서면 최종 패스) |
| 기록형 서면작성, "기록형 풀어", "서면 작성해줘", 변시 기록형 | 기록형풀이 (Skill) — 사건기록→서면(4.저작/대회 `프롬프트_서면작성_독립판` 연동) |
| v37 카드 빌드, "안키 카드 만들어", "apkg 빌드" | anki-card-generation (Skill) — outputs/02_cards_v37→apkg |

패키지 루트(2026-06-16 확인): `5.기타/프롬프트 등 개선/claude_code_package_v2/` — 표 내 `claude_code_package_v2/...`·`prompts/...` 참조는 모두 이 루트 기준 상대경로다.

주의: "학습 질문"과 "노트 편집/보강/검토 작업"을 구분한다. 노트 편집 요청에 소크라틱 모드를 적용하지 않는다.
주의: 신규 패키지(claude_code_package_v2)는 책 → 카드 자동화 프롬프트 묶음이고, 소크라틱(socratic.md)은 학습 모드. 별개 시스템으로 병행 운영.
주의: 운영 모드 — 01 OCR(LlamaParse 로컬)·02-wiki 위키화·02-card 카드화 모두 **Claude 단일 생태계**(폐기 도구 경위 → CLAUDE_변경이력.md). 원본 백업: 5.기타/패키지백업/. 산출물(#50-B): 쟁점 위키 → **sync/위키/{과목}/**, 사례노트 → sync/위키/사례/{과목}/, 원문 전문 → **outputs/**(비동기), 07 찌라시 → sync/찌라시/, 02-card·01_ocr → outputs/.
주의: 카드 대량 생성·룰 변경 시 운영 원칙(2026-06-12) — ①신규/변경 룰은 **1청크 파일럿**으로 검증(앞/뒤/빈칸 직접 확인) → ②통과 시에만 **전체 병렬 재작성**(워크플로우, 파일럿 통과한 작업은 사례형·암기장 등 병렬 동시 가동). 파일럿 없이 전량 실행 금지.

#18 Auto-Load Behavior: 맥락 감지 → 워크플로우/스킬 파일을 읽은 후 적용한다. 슬래시 명령 → 즉시 해당 파일을 읽고 적용한다.

파일 존재 확인: 표의 워크플로·스킬·패키지(prompts 16종) 전부 실재 확인 완료 — 확인 이력·목록은 `9.작업중/클로드/CLAUDE_변경이력.md`.

## 7. 진도 추적 (#19~#20)

#19 TOC-Based Progress: 교재 목차 경로 기준. 형식: {편}>{장}>{절}>{항목}

#19-B 진도 정본(2026-06-30 v2 갱신): **논점 frontmatter**(`sync/위키/{과목}/{쟁점}.md`의 회독·선택·사례·약점·진도, #50-A·#51). **인터페이스 병존(나+다)** = `6.진도관리/board_server_v2.py`(HTML, 데스크톱 입력) + `sync/위키/진도보드.base`(Bases, 열람) — 둘 다 같은 frontmatter 읽고 씀. 역기록 = `.agent/skills/daily-drill/scripts/mark_progress.py`. `progress.json`·`진도_현황.json`·`진도_state.json`(단원)은 **보조 snapshot/구 v1, 정본 아님**(v1 `board_server.py` deprecated → Bases 렌더 확인 후 _retired).

#20 Weak Problem Storage: .agent/state/learning.json → weak_points 저장. Spaced Repetition: .agent/state/srs_log.json에서 간격 관리 — **SM-2(Claude 중단기) → FSRS(안키 장기) 핸드오프**(전면전환 아님; 시험임박은 'cap' 아닌 deadline-aware desired-retention. 근거 9.작업중/클로드/개선종합_카드SRS_룰감사_2026-06-21.md).

## 8. 학습 행동 원칙 (#21~#25)

#21 Socratic First: 학습 질문(개념 이해, 문제 풀이) → /socratic 우선. 노트 편집/보강/검토 같은 작업 요청에는 적용하지 않는다. (학습 질문이 #17 자동 트리거·#48 표와 충돌하면 소크라틱이 우선)

#22 Label Restriction: "기출/빈출" 라벨은 소스에 표지가 있을 때만 사용하고 근거 2줄을 붙인다.

#23 Scoring Constraint: 배점표가 있으면 반영하고, 없으면 O/△/X만 사용한다.

#24 IRAC Block: 사례형 → 청구권/항변 블록, 근거 2줄을 포함한다. (청구-항변 2열 표 형식 세부는 #32로 일원화 — 형식은 #32 참조)

#25 Source Priority: 사례형 교재 > 사례형 자료 > 선택형 자료 > 정리 자료

## 9. 모드 제어 (#26~#28)

#26 Stop Word: "종료" → 현재 흐름을 종료한다.

#27 Auto/Manual Toggle: "자동"=약점 즉시 제시, "수동"=메뉴 질문.

#28 Only-on-Request: 전사/발췌는 명시적 요청 시에만 수행한다.

## 10. 코딩 규정 (#29~#31)

#29 Rule Consistency: 코드는 #1~#16과 일관되게 작성한다.

#30 Output Compliance: 법학 학습 관련 스크립트 출력에는 근거 2줄을 준수한다. 시스템 유틸리티(OCR, reflow, lint 등) 출력에는 적용하지 않는다.

#31 Code Integrity: 기존 코드 수정 시 기능을 보존한다. 주요 변경 전 테스트한다. (음성 전사 기능은 2026-07-02 폐기 — whisper·transcript 계열 스킬 `.agent/_retired/`, #49)

## 11~12. 노트·답안 형식 (#32~#37 — 모듈)

#32~#37 요지: 청구-항변=2열 표(원고|피고)(#32) · 판례 문구는 원문 표현만+사건번호(#34) · [[백링크]] 사용 — 판례·§10+·개념, 표/각주 제외, 연결사슬 약점→쟁점→원문(#35) · 교수님 표현 태그(#36) · 노트 본문 외국어 금지(#37). **답안·노트 작성 전 필독 → `.agent/rules/노트형식.md`**

## 13. Wiki 메모리 시스템 (#38~#41)

적용 범위: 모든 세션, 모든 과목

#38~#41 요지: 세션 시작 시 쟁점 위키(`sync/위키/{과목}/`) 관련 아티클 먼저 읽기(#38, wiki는 참고 — 교재 #1 우선) · 세션 종료 기록 .auto-memory(#39) · 아티클 판례·조문 소스 재확인(#40) · 정비 진입점 "메모리 정비"→memory-maintenance(#41). **정비·기록 작업 전 필독 → `.agent/rules/메모리정비.md`**

## 14. 파일 생성 위치·구글 산출물 (#42~#43, 2026-06-11 신설)

#42 파일 생성 위치(요지): 루트 생성 금지. **3층(2026-07-03, _meta 폐지)**: 데이터=`.agent/state/`(json·마커만) / 사용자 작업=`9.작업중/사용자/`(무단수정 금지) / **클로드 참고·정본+진행 문서=`9.작업중/클로드/`(+_INDEX 1줄 등록)** — 완료·사문화되면 5.기타/문서보관으로. 아카이브=`5.기타/문서보관/`. 검색 기본 스코프에서 문서보관·_trash·_retired 제외. 스크립트=.agent/scripts, 비노트 문서=5.기타/문서.
#43 구글 포인터(.gdoc 등)는 로컬 Read 불가 — Drive MCP로 열람. **생성·이동·구글 작업 상세 필독 → `.agent/rules/파일작업.md`**

## 15. 도구 가용성·충돌 방지 (#44~#47, 2026-06-11 신설)

#44/#46 요지: PDF 추출 전 **기존 md 추출본 우선(재추출 금지)** — outputs/01_ocr_llamaparse·02_cards_v37·위키 먼저 검색. 새 추출만 폴백체인(Read→pdfplumber→pdftotext→pypdf→수동). 상세 → `.agent/rules/파일작업.md`

#45 동시편집 충돌 방지: Edit 전 항상 Read · 충돌 위험 시 별도 파일명(_v2·_claude) · 외부 도구 흔적 발견 시 보고.

#45-B/C/D 요지(에이전트 운영 — **fan-out·대량작업·검증 전 필독 → `.agent/rules/에이전트운영.md`**):
- 판단=에이전트 / 결정적(해시·집계·마커·apkg)=코드(#45-B).
- 독립 단위 2+는 병렬 기본, 단 **메인 1단 fan-out만**(서브에이전트 하위 스폰 금지 #52-A) · 백그라운드 장시간 에이전트 금지 · 동시쓰기 격리(같은 파일 병렬 수정 금지, 공유파일 직렬).
- **대량 배치 = 영역별 소수 Agent(≤4~6)·내부 순차·완료마커(exists≠완료)·마커 재개**(#45-D). Workflow 대량 병렬은 예산 확보 시만.
- 법리·판례·조문 단정 산출물은 산출 직후 검증 패스(law_api verify-text·소스 대조·목차 누락0·백링크 무결) — 미확인 단정 삭제·보류(#2). 검증자는 보류·보고만(자동 수정·이동 금지). 안전레일(#16계열)은 모든 서브에이전트 동일 상속.
- 채용 게이트 산식·헤르메스 로그(staffing_log)·자기정합 6축(consistency_log) 상세는 모듈.

#47 산출물 동기화 (조건부):
주요 산출 노트 작성 후 wiki 진입점(sync/위키/_MOC.md, #50 정본)에 링크 추가(해당 토픽 존재 시), 세션 기록은 #39와 통합. 찌라시·카드 후보 추출은 #17 트리거(07·02-card 프롬프트)로 실행.

## 16. 플러그인·스킬 자연어 자동 발동 (#48, 2026-06-21 신설)

#48 플러그인·스킬 자연어 트리거: 설치된 플러그인·스킬은 별도 명령 없이 아래 한국어 자연어·맥락 패턴이 감지되면 자동 발동한다. 발동 = 해당 스킬 SKILL.md를 먼저 읽고 적용. 단, #1~#16 소스·삭제 규칙과 #21 소크라틱 우선이 항상 상위한다.

작동 원리(요약): 각 스킬·플러그인은 자체 트리거 설명을 갖고 있어, 사용자의 발화 의도가 그 설명과 맞으면 자동 선택된다. 영어 트리거가 기본이므로, 아래 표로 한국어 발화에도 동일하게 걸리도록 매핑을 고정한다.

| 한국어 자연어·맥락 패턴 | 발동 대상 | 비고 |
|----------|----------|------|
| "워드/docx로", "문서로 만들어", "보고서 파일", "메모/편지 파일" | docx (Skill) | 산출 위치 #42 |
| "발표자료/슬라이드/PPT/발제 슬라이드 만들어" | pptx (Skill) | |
| "엑셀/스프레드시트/표 파일/진도표/암기표 만들어" | xlsx (Skill) | |
| "PDF 합쳐/분리/병합/추출/양식채워/암호" | pdf (Skill) | 텍스트 추출은 #44·#46 우선 |
| "PDF 열어서 보여줘/주석/하이라이트/서명/양식 입력" | pdf-viewer (Plugin) | 교재·문서 시각 검토용. 단순 요약·추출은 Read |
| "포스터/도식/이미지 디자인/인포그래픽 만들어" | canvas-design (Skill) | |
| "심층 조사/딥리서치/여러 출처로 조사/검증된 보고서" | deep-research (Skill) | 학습 질문은 #21 소크라틱 우선 |
| "매일/매주/아침마다/정기적으로/리마인드/이따가/내일" | schedule (Skill) | 정기·예약 작업 |
| "메일에서 찾아/드라이브에서/일정/어디서 얘기했더라/여러 앱 한번에 검색" | enterprise-search (Plugin) + Google Drive·Gmail·Calendar MCP | 자료 검색. 결과는 #14 인용 |
| "할 일/태스크/오늘 일정 정리/업무 메모리" | productivity (Plugin) | 주의: 자체 진도관리(#19·daily-drill)와 중복 — 학습 진도엔 기존 시스템 우선, 일반 잡무만 사용 |
| "스킬 만들어/스킬 개선" | skill-creator (Skill) | |
| "문서 공동작성/스펙·제안서 같이 써줘" | doc-coauthoring (Skill) | |
| "피그마/UI 디자인/디자인 시스템/다이어그램(FigJam)" | figma (Plugin) | 법학 학습 무관 — 명시 요청 시에만 |
| "디자인/구조도/다이어그램/머메이드/mermaid/사실관계도/관계도/도식/PPT 디자인" | design-md (Skill) | E:\design_md MOC + 도식 규칙 — **mermaid TB(세로) 고정·PDF-safe·Fact_PPT 주체컬러·생성 후 검증 게이트**. 도식·다이어그램 작업 전 필독 |

직무 전용 플러그인 — 자동 발동 제외(명시 요청 시에만): legal(미국 in-house 법무: 계약 redline·NDA triage — 한국 로스쿨 학습과 무관하며 #1 소스근거 원칙과 충돌 소지), finance(회계·결산), data(SQL·데이터 분석), product-management(PM). 이들은 사용자가 플러그인명을 직접 부르거나 해당 직무 작업을 명시할 때만 사용한다.

우선순위 충돌 규칙: ①학습 질문(개념·문제 풀이)은 #21 소크라틱이 위 표보다 우선. ②자료 검색·인용 결과는 #1~#14(소스 근거·인용)를 준수. ③파일 이동·삭제는 #16·#16-B·#16-C가 항상 우선. ④"종료"(#26)·"수동"(#27) 시 자동 발동 중단.

## 17. 기능 수명주기·은퇴 프로토콜 (#49, 2026-06-22 신설)

#49 요지: 자산 수명주기 active→deprecated→retired. 은퇴=진입점 차단+`.agent/_retired/{날짜}/` 이동(가역·#16-C 로그·_RETIRED_INDEX). 실제 이동은 사용자 승인 후. **절차 필독 → `.agent/rules/메모리정비.md`**

## 18. 위키 닫힌루프·2층 분리·의미생성 (#50, 2026-06-26 신설)

#50/#51 요지(위키·카드 — **위키화·쟁점추출·사례노트·카드 작업 전 필독 → `.agent/rules/위키카드.md`**):
- **경로 정본**: 쟁점=`sync/위키/{과목}/`(원문 전체 보존+백링크, 요약 금지, 과목 약칭 금지) · 사례=`sync/위키/사례/{과목}/`(5섹션·포섭격자·모범답안 채점전용) · 원문 전문=`outputs/01_ocr_llamaparse/`(비동기) · 통권 PDF=`10.도서관`.
- **닫힌루프**: 목차(고정점)→쟁점(누락0)→카드(누락0)→드릴→보드(논점 frontmatter)→약점(learning.json)→목차. **키 표준 `과목|대분류|논점`**.
- **카드 3역할**: A실질 atom(전수·누락0 게이트) / 풀이순서 cloze / 사례 전문(카드화 비대상). **마커: `카드감사`=탐지 / `카드검증: 완료 (누락0, 날짜)`=완료 — skip·집계 기준은 카드검증만.** 암기장 선참고(중복 금지). 의미판단은 LLM(정규식은 후보 추출만).

## 20. 모델 티어링·채점관 3단·복습 3루프 (#52, 2026-07-01 신설)

#52 요지(티어링·채점·복습 — **드릴·채점·SRS 작업 전 필독 → `.agent/rules/채점복습.md`**):
- **모델 라우팅**: 결정적→T0 코드(모델 금지) / 탐색·목록화→**haiku** / 대량 생성·초벌채점→**sonnet** / 설계·게이트·종심→**fable** / fable 한도 시→**opus**. 최저 티어 시작, `애매`만 상향. **하위 에이전트 스폰 금지**(fan-out은 메인 1단만). 레인 ≤5(#45-D).
- **출제 단위(2026-07-04)**: 요건·효과=**세트 출제 필수**(개별 요건 파편 OX·단일 빈칸 금지 — 열거형·멀티클로즈·침입선다·미니사례 포섭형) / 판례=개별 OX 유지(변시 선택형 형식). 일 기본 --n 6(논점 6)·논점당 8문·복습 사례 3건(증량=문항 10·사례 4, 2026-07-06 확정). 세트=서브키 부분점수.
- **세션 구조(2026-07-06)**: ①오늘 논점=**혼동 클러스터 우선**(인접 법리) ②논점별 블록(개념→응용, 회독≥1은 개념 축소) ③**혼합 라운드**(판례OX·미니사례 논점 무표기 셔플 — 논점 판별도 답의 일부). 근거 조사보고_드릴출제구조_2026-07-06.
- **채점 4단**: G0 자가채점(번호만, 토큰~0) / G1 진리값(코드) / G2 초벌(Sonnet, 포섭격자 셀 대조) / G3 종심(Fable, 애매·실전·요청 시).
- **복습 3루프**: L0 필수복습(어제·오늘 본 논점=수업·드릴·보드체크 3원천, 재출제 필수) / L1 진도(회독0 선택형) / L2 장기(사례형+SRS→안키 FSRS, SRS due에 사례노트 있으면 사례형 미니 필수).
