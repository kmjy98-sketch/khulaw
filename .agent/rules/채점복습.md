# 룰 모듈: 채점·복습 (#52 전문 + #20 상세)

> 커널(CLAUDE.md)의 강제 포인터로 로드. 드릴·채점·SRS·복습 작업 전 필독.

#52 모델 티어링 + 채점관 + 복습 3루프 (근거 `9.작업중/클로드/개선설계_모델티어링_복습3루프_2026-07-01.md`·`개선설계_문서보관층_모델라우팅_2026-07-02.md`):
- **(A) 모델 티어**: T0 코드=진리값 대조·집계·dedup·마커·apkg(#45-B 결정적 커널) / **T1 볼륨=Sonnet**(카드 생성·보충, 사례노트 양산, OCR 1차, 초벌 채점 — Workflow/Agent에 `model: sonnet` 명시) / **T2 판단=Fable(세션 상속·토큰제)**(룰·설계, 누락0 게이트 표본검증, 사례형 종심, 품질 스팟검토, 정합성 검토). T1 산출의 `애매/판정불가` 플래그 항목만 T2로 에스컬레이션. **에이전트의 하위 에이전트 스폰 금지** — fan-out은 메인 오케스트레이터만.
  라우팅 표: 결정적→T0 코드(모델 금지) / 탐색·목록화·현황 스캔(얕은 판단·읽기전용)→**haiku** / 대량 생성·추출·교정·분류·초벌채점→**sonnet** / 설계·게이트·종심·스팟검토·정합검토→**fable** / fable 한도 중 판단 필요→**opus**(대체 판단자). 선택 원칙: ①코드로 되면 모델 금지 ②신뢰 가능한 최저 티어 ③`애매`만 한 티어 상향 — 높은 티어로 시작 금지. 팀 운영형: 오케스트레이터=세션 1 + 영역별 레인 ≤5(#45-D, 내부순차) + 게이트는 표본만 fable.
- **(B-0) 출제 단위 규칙(2026-07-04)**: **요건·효과 축은 세트 단위 출제 필수** — 요건 1개만 잘라 OX·단일 빈칸으로 내지 않는다(파편 금지). 유형: 세트 열거 재생형 / 멀티 빈칸 클로즈 / 침입형 선다("요건이 아닌 것") / 미니사례 포섭형. 예외=요건 자체에 판례 해석 쟁점이 있을 때만 개별 허용, 폴백=소스에 요건 열거 없으면 생략(#1). **판례 축은 개별 OX·판시 고르기 유지**(변시 선택형 실제 형식). 기본 문항수 논점당 8(일 기본 --n 6 기준 진도 트랙 총 48), 증량 10. 세트 문항은 요건별 서브키 부분점수(n/m), 약점 등록은 틀린 서브 요건 단위. 상세 daily-drill SKILL Phase 1.
- **(B-0-2) 세션 구조 3스텝(2026-07-06)**: ①논점 선정=혼동 클러스터 우선(같은 대분류·인접 법리 — 변별 대비: 인터리빙 이점은 혼동 가능 범주에서만) ②논점별 블록=개념→응용(회독≥1 재출제 논점은 개념 세트 1문 축소 — expertise reversal) ③혼합 라운드=판례OX·미니사례를 논점 라벨 없이 셔플(논점 판별=답의 일부, 블록 학습착각 차단). 근거 `9.작업중/클로드/조사보고_드릴출제구조_2026-07-06.md`. 상세 daily-drill SKILL §3-B.
- **(B) 채점관 3단 + 자가채점**: **G0 자가채점("번호만" 모드, 토큰~0)** = 문제 선정·번호·소스위치(책 p./사례노트명)만 브리프 출력(문제 본문 생성 생략), 사용자가 책 해설로 직접 채점 후 결과만 보고(`--mc 9/12`·약점명) → log_result 기록. / G1 진리값(코드·최소) = 선택형 정답키 선확정→진리값 대조. / G2 초벌(Sonnet) = 사례형 포섭격자 셀 대조(요건별 O/△/X+갭). / G3 종심(Fable) = G2 애매 플래그·실전답안·사용자 "종심" 요청 시만. 기본 라우팅: 학기중·토큰부족=G0, 평시 선택형=G1·사례형=G2, 선별=G3.
- **(C) 복습 3루프**: **L0 필수복습**(2026-07-06 확대 — **어제·오늘 '본' 논점은 복습 필수**: 원천 3종 = 수업 지정 `--today`→`최근수업` / 드릴 출제→`최근드릴` / **진도보드 체크→`최근체크`(서버 /api/bump·저장 시 자동 스탬프)**. 논점당 선택형 4~5문, 익일 재출제 후 L2 흡수) / **L1 진도**(회독0·진행 → 선택형, 진도 트랙) / **L2 장기**(회독≥1·약점 → 사례형 포섭격자+SRS, SM-2→안키 FSRS 핸드오프 #20 — **복습 3계층 정원제(2026-07-07)**: 약점 ≤4(초과분 SRS 익일 순환) + 목표 집합 3(방학·내신 현행 단원 — 혼동 인접 논점 간 변별 대비 인터리빙+successive relearning) + 전체 롤링 2(유지 리허설) = 최대 9논점, 계층별 정원 보장·상호 잠식 없음·후보 없으면 자동 축소. 정원 수치는 운영 설정값). 학기중 기본=L0+L1, 시험기=L2 확대(deadline-aware). **가변구도(2026-07-04, 07-06 상향·일기본 증량 확정)**: 평시(일 기본)=**--n 6(논점 6)·논점당 8문·복습 사례 3건** / 증량 트리거("증량/많이")=문항 10·사례 4건 (문항수·사례건수는 연구 도출이 아닌 운영 설정값 — 근거는 출제 '구조'에만 있음, 조사보고_드릴출제구조_2026-07-06) / **SRS due 항목에 사례노트 있으면 사례형 미니 1문 필수**(L2 사례 축) / 시험모드=learning.json exams D-21 자동(시험과목 우선, **D-14 사례억제** — 복습에서 사례형 생략·선택형/찌라시 위주). 운영 상세는 daily-drill SKILL.

#20 상세: 약점 원천 = `.agent/state/learning.json weak_points`(단일 정본). `srs_log.json`=현재 스케줄 상태, `srs_events.jsonl`=append-only 리뷰 이벤트 로그(2026-07-07 신설 — 연속 세션·재출제 지표 원천). **SRS 운영 결선(2026-07-07 감사 후 코드화)**: 드릴 마감 `log_result.py --srs-scores`가 SM-2 간격을 실제 갱신(미호출 시 SRS 정지 — 필수), 같은 날 반복=1세션 dedup, **간격 둔 3세션 연속 성공=자동 졸업**(graduated → 오늘 목록 제외 + `srs_graduated_queue.jsonl` 안키 증분덱 핸드오프 대기열), --add 중복은 자동 병합. 문항 단위 정오는 `drill_items.jsonl`(--items). **과목별 가중(2026-07-05 활성화)**: `learning.json['retention_factors']` = {과목: 승수} — 낮을수록 자주 복습(간격 ×승수) + 오늘 복습목록 우선 노출(정렬 2키, 1키는 priority 유지). 초기값 민법·민사소송법 0.85 / 형법(총론·각론 공통)·형사소송법 0.9 / 헌법·행정법·상법·민사집행법 1.0. 시험일 압축(deadline-aware)과 중첩 적용, 시험일 이후 예약 금지 캡은 항상 보존. 조정 = 값만 수정.
**successive relearning 운영 수치(2026-07-07, 근거 조사보고_드릴출제구조 §6-2)**: SRS 항목 졸업 = **간격을 둔 세션 3회 성공 → 안키 FSRS 핸드오프**(4~5세션은 수확체감). **같은 날 반복은 1세션으로 계산**(세션 분산이 파지를 결정 — 1일 3회 26% vs 3세션 분산 68%). 세션 내 오답 재제시 상한 2회(정답 시 큐 제거). 드릴 성과 지표는 당일 정답률이 아니라 **3~4주 후 재출제 정답률**로 판독. **SM-2(Claude 중단기) → FSRS(안키 장기) 핸드오프**(전면전환 아님; 시험임박은 'cap' 아닌 deadline-aware desired-retention. 근거 `개선종합_카드SRS_룰감사_2026-06-21.md`). mark_progress.py --weak/--clear-weak가 frontmatter와 learning.json을 동시 동기.
