# .codex

이 폴더에는 Codex Web/Pro에서 참고할 설정 예시만 Git에 포함한다.

실제 로컬 설정 파일은 Git 대상이 아니다.

- 포함: `config.example.toml`
- 제외: `config.toml`, `hooks.json`, 비밀값이 들어간 MCP 설정

로컬 설정을 공유해야 할 때는 실제 값을 제거한 뒤 예시 파일로 옮긴다.
