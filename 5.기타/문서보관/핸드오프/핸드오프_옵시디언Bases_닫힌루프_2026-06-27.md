---
type: 핸드오프
주제: 옵시디언 Bases 진도보드 + 닫힌루프 배선 (B안)
일자: 2026-06-27
병렬: 카드 작업(track 4)과 독립 — 동시 진행 가능
---

# 핸드오프 — 옵시디언 Bases 진도보드 + 닫힌루프 (B안)

> 이 문서만 보고 독립 진행 가능. 워크스페이스=`E:\법학볼트`(라이브). khulaw는 죽은 미러.

## 목표
localhost `board_server`(B 이전 대상)를 **옵시디언 Bases 네이티브 진도보드**로 대체하고, **닫힌루프(목차→쟁점→드릴→진도보드→약점→목차)**를 695 논점 키로 배선. 서버 없이 옵시디언이 진도/약점을 .md frontmatter로 저장·렌더.

## 현 상태(실측 2026-06-27)
- **695 논점 노트 완성**: `sync/위키/{과목}/{쟁점}.md` (과목=민법132·민소49·민집4·형법총론60·형법각론110·헌법106·행정234). frontmatter 키 = `type, 과목, 논점, 책, 원문, 상태(검증완료), 생성`. **진도 속성 없음.**
- **MOC(목차=고정점)**: `sync/위키/{과목}/_{과목}_목차.md` + `sync/위키/_MOC.md`. 논점 `[[ ]]` 링크.
- **.base 없음**(옛 _쟁점보드·_원문논점보드.base는 `_trash/2026-06-26`에). bases 플러그인 ON(core-plugins).
- **board_server**: `6.진도관리/board_server.py`(localhost:8770, 클릭→디스크쓰기). autostart(`C:\Users\111\AppData\Local\jindo_board_autostart\autostart.py`) 경로 E:로 고침됨.
- **닫힌루프 노드**: `진도_state.json`(202단원 회독/선택/사례/기록), `learning.json`(weak_points), `srs_log.json`(4), **`drill_log.jsonl`=0B(빈)**.

## 작업 (마커·로그 구조 의무 — #45-C)
1. **논점 노트 진도속성 배선**: 695 노트 frontmatter에 `회독:0, 선택:0, 사례:0, 기록:0, 약점:false, 최근복습:"", 진도:"미착수"` 추가(배치+완료마커, 결과 파일기록). 기존 키 보존.
2. **`진도보드.base` 작성**: 뷰 ①표(과목별 논점+회독/선택/사례/기록/진도) ②보드(진도 또는 약점 칸반) ③약점(약점=true·복습due 필터). source=`sync/위키/{과목}`, group/sort by 과목·논점.
3. **시드 이관**: `진도_state.json`(202 단원키 subj|중분류|단원) → 논점 노트 진도속성으로 의미매핑(불명확=0). `learning.json` weak_points → 해당 논점 `약점:true`.
4. **drill·SRS 배선**: `drill_log.jsonl` 시드, daily-drill이 논점 키로 출제·기록, srs_log 논점 키 정합.
5. **board_server 은퇴**(#49): 위 완료·검증 후 `_retired` 이동, autostart 비활성. (그 전까진 유지)

## 제약(필수)
- #50: 쟁점=`sync/위키/{과목}`(sync), 원문=`outputs/`(비동기). #16: 삭제금지→`_trash`. #16-C: 이동 시 `log_file_op.py`.
- #45-C: 대형작업 = 완료마커 + **결과 파일기록(반환만 의존 금지)** + 현황로그(`.agent/state/bases_진행.md` 같은) + 재개 시 마커 skip.
- PDF에 파이썬 금지(플러그인/`pdf` 스킬). 김준호 저서 제외.
- 세션/월 한도 자주 닿음 → 배치+마커로 재개. 진행로그 스크립트 권장(예 `_rebuild_status.py` 패턴).

## 병렬 안전
카드 작업(outputs/02_cards)과 충돌 없음 — 본 작업은 `sync/위키` frontmatter + `.base`만 건드림. 공유파일(_MOC·master 로그)은 직렬.
