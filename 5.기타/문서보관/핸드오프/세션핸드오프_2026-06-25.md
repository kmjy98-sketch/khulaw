---
type: 핸드오프
일자: 2026-06-25
브랜치: dev/codex-rules-cleanup (최신 c13decb, origin 동기화). main d92eb87 안정 유지
이전: sync/_meta/옵시디언_안키_통합_검토_핸드오프_2026-06-23.md (PR#1, dev 통합됨)
---

# 세션 핸드오프 2026-06-25

## 0. 한 화면
이주·자급화·신경망 복습연구·카드 파이프라인 정상화·PR#1 적응형학습·4개 PR dev 통합·S4·자소서 정본화·위키 백로그 초안까지 진행. **즉시 후속 = 위키 7개 아티클 fix-round(판례 사건번호 교정).**

## 1. 이번 세션 완료
| 영역 | 결과 | 위치 |
|---|---|---|
| 이주·자급화 | vault.json E:, `law_api.py`(korean-law MCP #49 은퇴), 죽은참조 20+파일 정리, H:\ 스크립트 E: 숫자폴더 | .agent/lib·scripts |
| 신경망 복습연구 | "모델교체 불요, FSRS 유지 + LLM 계층" + **S1~S6 설계** | [[신경망_ML_복습기법_연구_2026-06-25]] |
| 카드 파이프라인 | 김기용 3파일 수정(241장), **apkg 24,576**(한자0·번호없는cloze0·안정guid), TSV 동기화 8,935→24,380, 위키 인벤토리 312주제 | outputs/anki, .agent/state |
| PR#1 적응형학습 | cp949 데모 크래시 수정(작동 실증) + 실연동 글루 스펙 | [[적응형학습_실연동_글루스펙_2026-06-25]] |
| S4 보강 | `retention_factor`(과목별 desired-retention, 기본 1.0=무변화) | srs_scheduler.py |
| 자소서 정본화 | 작성법소스→정본 + 로더 SKILL `자소서작성` + 3변형 _retired | sync/_meta/자소서_작성가이드, .agent/skills/자소서작성 |
| 위키 백로그 | top8 쟁점 아티클 초안(7개 검토필요·1개 통과) | sync/wiki/쟁점, [[위키백로그_검증결과_2026-06-25]] |

## 2. ★즉시 후속 (우선순위)
1. **위키 fix-round** — sync/wiki/쟁점 7개 아티클(검토필요 배너) 판례 anchor 교정: ①재판형식(가사비송 '스'=대결, '다/도/므'=대판) ②[검증필요] 일관 부착(law_api verified:false 전부) ③번호변형 교정(므↔스 등 카드 원문 대조) ④외부지식 제거(카드 없는 조문 항 열거). 가이드=`위키백로그_검증결과_2026-06-25.md`. 교정 후 `sync/wiki/_index.md` 공개(#47).
2. **SRS 루프 가동** — drill_log/srs_log 시드(현재 빈·rep0). 가동 후 `learning.json["retention_factors"]`에 핵심과목 0.85 설정(S4) → S1·S2·S6 등 나머지 보강.
3. **자소서** — 정본(작성법소스) 내용 검토 + CLAUDE.md #17/#48 표에 "자소서작성" 1행 등록.
4. **위키 백로그 잔여** — 312주제 중 89 아티클(4%). 다음 후보: 추후보완·소송이송 외 카드 많은 순.

## 3. 중기/저우선
- outline due #4 결정(목차회상 오답 재출제 D+1 vs D+3, PR#1 D+3 잠정).
- E 누락 `.claude` 스텁 10종(dispatch·daily-drill 등 #17/#48 진입점).
- TSV 196장 잔차(24,380 vs 24,576 = dedup·분기 미세차).
- 적응형 실연동 글루 **구현**(런타임 데이터 후 — problem_index·learning.json).

## 4. 핵심 위치
- 룰 `CLAUDE.md`(#1~49). 스킬 `.agent/skills`(30+). 상태 `.agent/state`(gitignore).
- 카드 `outputs/02_cards_v37`(미추적) → `build_v37_apkg.py`/`build_v37_tsv.py` → `outputs/anki/v37`.
- 위키 `sync/위키/원문`(원문 정본 25책) · `sync/wiki/쟁점`(롤업 89+). 인벤토리 `.agent/state/wiki_topic_inventory.json`.
- 문서 `sync/_meta/`(신경망연구·복습spec·글쓰기설계·PPT·폴더도·글루스펙·검증결과).
- git 정책: [[git-workflow-dev-consolidation]] — PR은 dev 통합, main 직접푸시 차단(classifier).

## 5. 이번 세션 교훈
- **클라우드(Linux UTF-8) 산출 스크립트는 윈도우 cp949 콘솔에서 유니코드 출력 크래시** → `sys.stdout.reconfigure(encoding="utf-8")` 가드 필수.
- **LLM 위키화는 판례 사건번호 재판형식을 틀린다**(스↔대판·번호변형) → 적대검증 + `law_api.py verify-case` 필수, 미통과는 [검증필요].
- **런타임 산출물(TSV·drill_log·srs_log·inventory)이 비어있던 근본원인** → 카드 빌드 1회로 대부분 해소.
- 파이프라인 동기화: 빌더 둘(apkg·tsv)은 `deck_of`/`blocks_of` 단일출처 재사용으로 정합.
