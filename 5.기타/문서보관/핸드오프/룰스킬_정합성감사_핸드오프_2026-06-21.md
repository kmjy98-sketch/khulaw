# 룰·스킬 정합성/효율성 감사 — 핸드오프 문서

> 작성 2026-06-21 · 목적: **다른 대화 세션에 그대로 넘겨 후속 작업을 이어가기 위한 자기완결형 브리핑**
> 이 문서만 읽으면 맥락 없이도 무엇이 문제이고 어떻게 고치는지 알 수 있도록 작성함.

## 0. 이 문서로 다른 대화에서 할 일

1. 아래 **High 4건**을 우선 검토한다.
2. H1~H3, M1·M2·M4는 **CLAUDE.md / AGENTS.md 문구 수정만**으로 해소된다(파일 이동 불필요 → #16-B 무관). 편집 전 변경 목록(파일+룰번호+전후)을 사용자에게 먼저 제시하고 승인받는다(#15·#5).
3. H1의 **물리 경로 통합**(`sync/wiki` ↔ `sync/위키`)과 Low의 **파일명 손상 정규화**는 파일 이동/리네임이라 별도 승인 + `log_file_op.py` 로그(#16-C) 필요.
4. 모든 수정은 "지어낸 경로 추가"가 아니라 "이미 결정된 최신 상태로 룰 본문을 따라가게 하는" 작업이다(근거 메모리 파일은 각 항목에 명시).

## 1. 한 줄 결론

`path_fabrication` 없음(참조 경로 ~93% 실재). 모든 문제는 **"메모리·코드는 갱신됐는데 CLAUDE.md/AGENTS.md 본문만 노후화"된 드리프트**. 근본 원인은 동일 규칙의 3중 중복(글로벌·CLAUDE·AGENTS).

## 2. 검증 방법/상태

- CLAUDE.md(`H:\내 드라이브\CLAUDE.md`), AGENTS.md, 글로벌 `C:\Users\111\.claude\CLAUDE.md` 원문 대조.
- `.agent/skills/*/SKILL.md`·`.agent/workflows/*` 인벤토리, CLAUDE.md #17/#48 트리거표와 교차.
- CLAUDE.md 참조 경로 전수 존재 확인(파일시스템).
- 메모리(`C:\Users\111\.claude\projects\H--------\memory\`)의 최신 결정 vs 룰 본문 대조.
- 4개 병렬 감사 에이전트 결과를 교차검증해 중복 제거·과대평가 보정함.

## 3. High — 구조적 모순 4건

### H1. 위키 정본 경로 이원화 + 논점 수치 불일치
- **현행 룰**: CLAUDE.md #38 = 정본 `sync/위키/원문/`(한글). 그러나 #46·#47 및 AGENTS.md #42 = `sync/wiki/`(영문)를 진입점으로 가리킴. 논점 수: 룰 "1,156논점" vs 실제 `sync/위키/원문/_index.md` "25책 **1,130단위**".
- **사실**: 두 경로(한글/영문)·두 `_index.md` 모두 실재. 정본=한글 `sync/위키/원문` 으로 확정됨.
- **근거 메모리**: `project_wiki_jindo_2026-06.md`, `project_pkm_redesign_2026-06.md`.
- **권고**:
  - #38·#46·#47·AGENTS.md #42의 진입점을 **`sync/위키/원문/_index.md`(한글)** 으로 통일.
  - "1,156논점" 같은 변동 수치는 룰에서 삭제하고 `_index.md`를 단일 출처로(원하면 "약 1,130단위"로 정정).
  - 물리적 경로 통합(영문 폴더 → 한글로 병합)은 **사용자 승인 + 로그** 후 별도 수행. CLAUDE.md #40 후주 "위키/wiki 명명 통합은 추후 승인 후" 가 이 미결을 이미 명시.

### H2. Colab·Antigravity·Gemini Gems 폐기 미반영
- **현행 룰**:
  - #13: OCR 3단계가 "①Colab `ocr_extract_v2.ipynb` → ②Colab `ocr_compare_v2.ipynb`"를 핵심 흐름으로 명시("Colab은 무료").
  - #17 주의: "안티그래비티 앱 = 01 OCR + 02-wiki 위키화 / Opus = 카드화" 2모델 분담.
  - #43: 구글문서 집결 예외에 "Gemini Gems·Colab Notebooks 루트 보관".
  - #45: 동시편집 충돌 예시로 "Antigravity, Cursor".
- **사실**: Colab·Gems·Antigravity **폐기**. OCR=LlamaParse 로컬, 위키화=Claude Code로 환원.
- **근거 메모리**: `project_pkm_redesign_2026-06.md`.
- **권고**:
  - #13 OCR 파이프라인을 LlamaParse 로컬 기준으로 재서술, Colab 노트북 2종은 레거시로 강등(이동·이름변경 금지 주석은 별개로 유지).
  - #17 주의문에서 안티그래비티 분담 삭제, 위키화 주체를 Claude Code로.
  - #43 예외 목록에서 Gems·Colab을 제거하거나 "폐기됨"(Opal과 동일) 표기.
  - #45 예시 도구 갱신(경미).

### H3. 카드 정본 v37 미반영
- **현행 룰**: #46이 `outputs/02_cards/`를 "카드화 완료분(주제:: 태그 grep)" 1차 검색처로 지정.
- **사실**: 정본 = `outputs/02_cards_v37/`. `outputs/02_cards/`는 폐기예정 부분 intake. **코드는 이미 v37 전환 완료**(`build_session.py` L27·L137, daily-drill `SKILL.md` L165).
- **근거 메모리**: `project_card_corpus_unify_2026-06.md`.
- **권고**: #46 검색 대상을 `outputs/02_cards_v37/`로 교체, `02_cards`는 "폐기예정" 주석. (룰만 따라오면 코드와 정합 회복.)

### H4. 스킬 레지스트리 갭 (자동발동 리스크)
- **현상**: `daily-drill`·`file_ops_log`·`기록형풀이`·`textbook-reflow`·`anki-card-generation` 등이 **세션 노출 스킬 목록에 부재**. SKILL.md frontmatter는 정상(예: daily-drill name/description 유효). git상 신규 untracked.
- **권고**: 세션 재시작/레지스트리 재로딩 후 노출되는지 확인. 계속 누락이면 frontmatter 형식·스킬 폴더 위치 점검. (CLAUDE.md #17이 daily-drill을 핵심 루틴으로 전제하므로 실제 발동 여부 확인이 중요.)
- **주의(과대평가 보정)**: 스킬은 자체 description으로 자동발동(#48 원리)되므로 *#17/#48 표에 없음 ≠ 발동 안 함*. 실제 발동 리스크는 이 H4(레지스트리 부재)에 국한. 표 미등록 스킬 다수(law-note-supplement·spaced-repetition·progress-tracker 등)는 발동되지만 문서화만 빠진 것.

## 4. Medium

| # | 항목 | 현행 | 최신 결정 / 권고 | 근거 메모리 |
|---|------|------|------------------|-------------|
| M1 | SRS 방식 | #20 "SM-2"로 단정 | "SM-2(Claude 중단기) → FSRS(안키 장기) 핸드오프"로 수정. 전면전환 아님 명시 | `project_cards_srs_upgrade_2026-06`, `project_card_corpus_unify_2026-06` |
| M2 | 진도 입력 | #19/#20에 자동추적 뉘앙스, 수동입력 미명문 | "progress.json 수동 갱신, 전사문 자동추적 폐기·재구축 금지" 명문화(미래 세션 안전장치) | `feedback_progress_manual` |
| M3 | 노트 트리거 모호 | #17 "노트정리/수업정리"→`lecture-notes.md`(워크플로 문서일 뿐) | 실행 스킬 `study-notes` 명시 + `lecture-notes.md`는 참조 워크플로로. `law-note-supplement`("노트 보완")와 경계 명확화 | — |
| M4 | AGENTS 고유규정 공백 | AGENTS.md만 있는 RAG 2.0이 "CLAUDE.md 우선" 탓에 효력 모호 | "CLAUDE.md 미규정 영역에서 보충 적용" 명문, 또는 핵심 규정 CLAUDE.md 승격 | — |
| M5 | 트리거 키워드 중복 | "정리"(study-notes·기록형풀이·글다듬기), "파일 정리"(file-classification·file_ops_log), "OCR 교정"(transcript-correction·textbook-reflow) | 키워드 분담·우선순위 명시 | — |
| M6 | 규정 3중 중복 | 삭제금지·`_trash`·이동로그가 글로벌·CLAUDE·AGENTS 3곳 전문 중복 → 드리프트 원인 | 글로벌=원칙 1줄 / CLAUDE=세부 / AGENTS=미러로 역할 분리, 중복 본문 축소 | — |
| M7 | 강제수단 미명문 | #16 계열이 자율 준수에만 의존 | "PreToolUse 훅으로 #16/#16-B/#16-C 강제 가능(권고)" 주석(코드 도입 별도) | `project_pkm_redesign_2026-06` |

## 5. Low

- CLAUDE.md #7 제목 "COT **Disclosure**"가 내용(비노출)과 반대 → "Non-Disclosure"로(AGENTS.md와 일치).
- 헤더 "총 48개(#1~#47)" 부정확(#48 존재). 버전라인 v3.2가 2026-06-21 갱신 미반영 → 버전 bump.
- 백링크 §1~§9 제외 규정이 CLAUDE.md #35엔 있고 AGENTS.md #39엔 없음 → AGENTS에 단서 추가.
- 글로벌 룰 "셸=bash/Unix 강제" vs 실제 PowerShell 1차 환경 → 글로벌 셸 가정 갱신.
- `outputs/02_cards_v37/` 일부 파일명에 이중 절대경로(`H/내 드라이브/outputs/01_ocr/...`) 손상 → 파일명 정규화(이동 작업, 승인+로그 필요). **추가 확인 권장**(클라우드 placeholder 오독 가능성 배제).

## 6. 효율성 관찰

- **중복**: M6의 3중 규정 + 수동 트리거 표(스킬 self-trigger와 이중화)가 유지보수 비용·드리프트 원인.
- **사장 자산**: `.agent/workflows/` 15개 중 12개가 룰 미참조(레거시 추정: `card-wiki-pipeline`·`colab-pipeline`·`legal-suite`·`law-study-automation` 등) → 정리 또는 등록 결정 필요.

## 7. 적용 가드레일 (다른 대화가 반드시 지킬 것)

- 룰 본문 수정 = CLAUDE.md/AGENTS.md 텍스트 편집(파일 이동 아님). 2개 이상 파일이면 #15 변경 목록 선제시.
- **공유드라이브(`0.공유드라이브/`) 및 위키 물리 경로 이동은 #16-B 우선** — 사용자 명시 승인 전 금지.
- 파일 이동/리네임 발생 시 `.agent/scripts/log_file_op.py`로 `master.{csv,jsonl,md}` 기록(#16-C).
- 삭제 금지 — 필요 시 `_trash/{YYYY-MM-DD}/`(#16).

## 8. 미해결 / 추가 검증 필요

- H4 레지스트리 부재의 정확한 원인(신규 untracked vs frontmatter) — 세션 재시작 후 재확인.
- Low 파일명 손상이 실제 디스크 파일명인지 클라우드 동기화 placeholder인지 — 직접 확인.
