---
type: 마스터실행계획
주제: PKM 재설계 실행 — 검증완료 단일 체크리스트
일자: 2026-06-21
상태: 실행 대기(NOW 묶음 승인 후 착수)
supersedes(실행기준): _핸드오프_PKM재설계 · 세컨드브레인_재설계_근거기반 · 개선종합_카드SRS_룰감사
검증근거: 포그라운드 조사 8 + 검수 3 (코워크 vs Code: task wx9r3cz0o / 카드·SRS·학습: wg55q18yr / 내부감사: w30ln00sj)
---

# PKM 마스터 실행계획 (검증완료)

> 원칙: 비파괴(#16, _trash 이동), 공유드라이브 불가침(#16-B), 이동=log_file_op.py 로그(#16-C), 2개↑ 변경은 승인 후. 추측 아닌 실측 기반.

## 0. 아키텍처 확정 — Cowork 제거

코워크 고유 우위 **없음**(공식출처 적대검증, task wx9r3cz0o). 백그라운드·스케줄은 오히려 디바이스 종속이라 열위.
- **Claude Code CLI(로컬)** = 정본 볼트(E:)·MCP·결정적 파이썬(OCR/카드/진도/사례채점 I/O)·파일이동 [주력]
- **Claude Code 웹/Routines(클라우드)** = 원격·비동기·무인 스케줄·폰 모니터링 [보조]. 단 **GitHub repo 기반** → 비-깃 로컬 볼트는 직접 못 다룸 → 로컬 볼트 파이프라인은 CLI 전용.
- **데스크톱 Code(computer-use)** = 한글(HWP)·Drive 웹 UI 등 GUI 조작만 [예외]
- **Cowork = 불필요**

## 1. 검증된 현 상태 (실측)

- 위키 정본 = **`sync/위키/원문/`**(한글, 1,156논점, wiki_원문분할.py OUT_BASE 코드확인). `sync/wiki/원문`=신규 31개, `sync/wiki/쟁점`=36개. CLAUDE.md #38·#40·L120 = **불일치**(룰은 sync/wiki만 가리킴).
- H:\ 하드코딩 = **~91파일/~267라인**(251 아님), .agent/scripts가 70%. vault.json+_vault.py 인프라 **동작가능**, 64개 .py 중 14개만 적용(50개 남음).
- 카드 정본 = **outputs/02_cards_v37**(310). build_v37·daily-drill 둘 다 v37 지정 ✓(누락위험 낮음). note_key 안정·검증완료. 병목=위키 쟁점롤업 36/89 미완.
- 진도 정본 = **progress.json**(스크립트 실사용), 진도_현황.json=대시보드 snapshot.
- 사례채점 = **하이브리드**: Python(coverage 초벌 O/△/X) + LLM(가중치 루브릭·E1~E3·결론캡 = SKILL.md L99가 코드미반영·수동 인정).
- SRS = SM-2, interval=1 고정·초기 due 미분산. drill_log.jsonl 0건(미활성). weak 4건.
- 거버넌스 = CLAUDE.md 289줄/48룰. 훅=SessionStart 1개만. **PreToolUse 훅 공식지원(강제 실현가능)**. frontmatter 빈약(메타는 인라인 과목::속성::주제::), Bases는 YAML만 질의 → 표준화 선결.
- frontmatter: 카드 `주제::`는 grep 계약(제거불가) → YAML 병기 전략.

## 2. NOW 실행 묶음 (확정 스코프·검증완료·승인 후 즉시)

확정 스코프 = 데이터무결성 + 파일위치·룰충돌 + 최신연구보완(FSRS 전면전환 제외).

### N-A 데이터 무결성 (코드)
- `log_file_op.py`에 파일잠금 추가(`msvcrt.locking()`/`portalocker`, ※fcntl 불가) — 락 `.agent/file_ops_log/.master.lock`. 수정 후 1회 테스트.

### N-B 위키 경로 룰 정정 (HIGH·검증완료, 텍스트)
- CLAUDE.md #38(L196)·#40(L215)·L120·#17표 → 정본 `sync/위키/원문/` 명시. `sync/wiki/원문`=신규 staging·`sync/wiki/쟁점`(#46) 구분. (실제경로 검증됨)

### N-C 파일위치 정리 (이동안전 검증완료, 각 log_file_op.py --execute)
- .gdoc 3개 → `5.기타/구글문서/` · 예선 hwp 2개 → `7.대회/` · 스타터킷 PDF → `5.기타/문서/`
- temp_pdf_extract.md·temp_song_casebook.md → `_trash/2026-06-21/`
- `1.민사 - 복사본/` → 대조 후 trash · `sync/노트`(빈) → trash
- 보류: `.agent/` 루트 dryrun/scan/log json 7개 → 활성참조 0이나 안전마진 위해 `.agent/_archive/`로(삭제 아님). 19GB `5.기타/_백업`=보존.

### N-D 룰 충돌 텍스트 정합 (CLAUDE.md)
- #16에 "공유드라이브는 _trash도 금지(#16-B 우선)" · #43 집결=루트 포인터만(공유드라이브 제외) · #24↔#32 중복통합 · #38 wiki "조회 먼저/근거는 #1" 이중성 정리 · #21에 "#17보다 우선(L289)" · daily-drill(출제)↔case-answer-review(채점) 경계 1줄 · korean-law-mcp 8스킬 graceful degrade.

### N-E 최신연구 보완 (룰/스킬 메모, 코드 아님)
- 카드: 원자 cloze(요건·판례) + 종합/IRAC 카드 병행(far-transfer) + 유사항목 대조단서(간섭) + **LLM 카드 12결함 검수 카탈로그**(human-in-loop) → 카드 스킬/룰 반영.
- SRS: SM-2 유지 + **'시험임박 cap' → deadline-aware desired-retention** 표현정정 + 초기 due 분산(1/3/7) 메모 + **Claude 중단기 SM-2 → Anki 장기 FSRS 핸드오프** 명시(이중관리 아님).
- 사례채점 하이브리드 경계(Python 수집·초벌 / LLM 채점) 룰 반영.

## 3. P1 (저위험·승인 후)
- VAULT_ROOT 디커플: 남은 50개 .py를 _vault.vp() 전환(env로 선검증). 
- frontmatter Master Property Schema 정의 → 1과목 파일럿 → Bases 보드 1개.
- 링크 무결성: orphan/broken 플러그인 + rename 자동갱신 + fix-renamed-links 헬퍼.
- 주간리뷰 스킬 신설(최대 갭): 주간 약점집계·시험 역산 페이싱·다음주 과목별 3토픽.
- 위키 쟁점롤업 재개(36→89, 포그라운드 서브에이전트).

## 4. P2 (중위험·승인)
- 절대룰 **PreToolUse 훅** 강제(#16 rm/del/Remove-Item deny · #16-B 0.공유드라이브 write deny · #16-C move 전 로그) — Windows Git Bash. settings.json hooks.PreToolUse.
- colab·gems·antigravity 폐기: `.agent/notebooks/`·Gemini Gems → `_trash`. OCR=LlamaParse 로컬 단일로 #13 갱신, 위키화=Code로 #17 갱신.
- CLAUDE.md 다이어트(상세→references/skills).

## 5. P3 (방학·고위험·사용자 GUI)
- E: 이주 컷오버: 앱종료→Drive 동기화 0확인→E: 복사(원본보존)→검증→VAULT_ROOT 변경→Drive 동결+rclone copy 백업→폰 선택동기화. 시험기간 금지. 공유드라이브 미접촉.

## 6. 미결정(사용자 확인)
- N-C 이동 승인 / 위키 명명 통합(파일이동) 여부 / P2 PreToolUse 훅 도입 / frontmatter 파일럿 과목 / E: 경로·시점(방학).

## 7. 스타터킷 채택 결론
- Brad(goal cascade)=기각(법학 불일치·기존커버). Noah=부분영감(frontmatter 자동태그·thinking-partner). **자동삭제·git add -A·크로스세션 메모리 회피**(#16/유출).
