# 보완 기록: problem-index 약칭 박멸

작성일: 2026-06-30

---

## 변경 대상 파일

1. `E:\법학볼트\.agent\skills\problem-index\scripts\scan_problems.py`
2. `E:\법학볼트\.agent\skills\socratic-core\docs\subject-detect.md`

---

## 변경 요약

### 1. scan_problems.py — `detect_subject` 함수 (구 L63~L73)

| 구분 | 이전 | 이후 |
|------|------|------|
| '민소' 매칭 반환값 | `'민소법'` (약칭) | `'민사소송법'` (풀네임) |
| '형소' 매칭 반환값 | `'형소법'` (약칭) | `'형사소송법'` (풀네임) |
| '민사소송' 선검사 | 없음(누락) | 추가 — '민사' 이전에 선검사 |
| '민사집행' 분기 | 없음(누락) | '민사집행' / '민집' 키워드 → `'민사집행법'` |
| '형사소송' 선검사 | 없음(누락) | 추가 — '형법'·'형사' 이전에 선검사 |
| 형법총론 분기 | '형법'·'형사' → `'형법'` (단일) | '형법총론'·'총론' → `'형법총론'` |
| 형법각론 분기 | 없음(누락) | '형법각론'·'각론' → `'형법각론'` |
| 헌법 분기 | 없음(누락) | '헌법' → `'헌법'` |
| 행정법 분기 | 없음(누락) | '행정법' → `'행정법'` |

#### 검사 순서 (변경 후, 오분류 방지)

1. '민사소송' / '민소' → 민사소송법
2. '민사집행' / '민집' → 민사집행법
3. '민법' / '민사' → 민법
4. '형사소송' / '형소' → 형사소송법
5. '형법총론' / '총론' → 형법총론
6. '형법각론' / '각론' → 형법각론
7. '형법' / '형사' → 형법총론 (폴백)
8. '헌법' → 헌법
9. '행정법' → 행정법
10. 해당 없음 → None

**주의**: '민사' 키워드가 '민사소송법'·'민사집행법' 경로 파일에도 포함될 수 있으므로, 세부 과목을 먼저 검사하는 순서가 오분류를 방지한다.

### 2. subject-detect.md — 표제 약칭 교체

| 위치 | 이전 | 이후 |
|------|------|------|
| 감지 신호 표, 3행 | `**민소법**` | `**민사소송법**` |
| 감지 신호 표, 4행 | `**형소법**` | `**형사소송법**` |
| 과목별 분석 구조 H3 | `### 민소법` | `### 민사소송법` |
| 과목별 분석 구조 H3 | `### 형소법` | `### 형사소송법` |

---

## 보존 사항 (변경 없음)

- `detect_problem_type`, `scan_directory`, `main` 등 기존 로직 전부 보존
- `민법`, `형법(총론 폴백)` 반환 경로 기존 동작 유지
- `subject-detect.md` 분석 구조 내용(절차 설명) 보존 — 표제·표 셀만 교체

---

## 적용 근거

- CLAUDE.md #50-B: 과목 폴네임 강제 (`민소법`/`민집법` 약칭 금지)
- CLAUDE.md #51: 키 표준 `과목|대분류|논점` — 과목 = 풀네임
- CLAUDE.md #19-B: board_server_v2 / mark_progress.py 키 통일 (`과목` 필드 풀네임)
