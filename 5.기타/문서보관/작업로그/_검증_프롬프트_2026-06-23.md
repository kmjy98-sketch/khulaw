# E:\법학볼트 이주 검증 — 새 세션 프롬프트

> 새 Claude Code 세션을 **E:\법학볼트** 에서 열고, 아래 `---` 사이 블록을 그대로 붙여넣는다.

---
이주 검증해줘. 순서대로 하고 결과를 표로 보고:

1. **vault.json**: `python .agent\scripts\_vault.py` → `VAULT_ROOT`가 `E:\법학볼트` 로 출력되는지.
2. **매니페스트 대조**: `powershell -ExecutionPolicy Bypass -File "sync\_meta\_verify_migration.ps1"` 실행 → **MISSING=0, CHANGED=0** 확인 (기준 17,212파일, .git 제외).
3. **핵심 보존 스폿체크**(존재+개수):
   - `1.민사\송영곤_재산법교재` = 4개 파일 (논점민법강의_재산법_26.pdf / 민사법쟁점노트_재산법_26.pdf / 2025 논점민법강의-재산법(분철-1).pdf / 2026_논점민법강의-재산법-제12판.zip)
   - `5.기타\프롬프트 등 개선\claude_code_package_v2` 존재
   - `sync` · `.agent` · `outputs` · `7.대회` 존재
4. **핵심 스크립트 로드**: `log_file_op.py` · `build_v37_apkg.py` 가 에러 없이 import 되는지.

MISSING/CHANGED가 있으면 그 파일 목록을 제시하고, `5.기타\_E드라이브_고유백업_2026-06-22`(H: 스트림 종료 후엔 Drive 웹) 또는 원본에서 복구 안내. 이상 없으면 "이주 무결성 OK"로 마무리하고, 다음 보류작업(E: 백업체계 rclone/git → frontmatter 확대 → 쟁점 파일명 정리)으로 넘어갈지 물어봐.
---

## 검증 자산 (이 폴더)
- `_검증_매니페스트_2026-06-23.csv` (한글명) = `_migration_manifest_2026-06-23.csv` (동일 ASCII 사본) — 17,212파일 기준선(.git 제외)
- `_verify_migration.ps1` — 매니페스트 vs 현재 E: 자동 대조(MISSING/CHANGED/EXTRA)
- `_검증_E대H_2026-06-23.csv` — **H: 원본 vs E: 완결성 대조**(스트림 종료 전 1회 실행분; DIFF=원본보다 적게 복사 의심)
- `_이주_FINAL_핸드오프_2026-06-22.md` — 전체 인계 문서
