---
type: 보완계획
주제: 룰·스킬 현황검토 종합 → 새 구조(#50/#51/v2보드/과목풀네임) 전파 보완
일자: 2026-06-30
근거: .agent/state/검토_R1_CLAUDE.md · 검토_R2_학습루프.md · 검토_R3_카드노트보드.md · 검토_R4_충돌죽은참조.md
---

# 룰·스킬 보완 계획 (현황검토 종합)

## 진단 (한 줄)
새 구조는 **데이터·CLAUDE.md #50/#51**엔 반영됐으나 **의존 스킬·교차참조·런처·산출물엔 미전파** → v1/v2·단원/논점·원문경로·마커명·약칭 **이중잔존**. = 만성 회귀병(지시 미영속화)의 재발. 보완 = 새 구조를 전 의존처에 전파 + v1 은퇴 + 마커/키/경로 단일화.

## A. 진도보드 — 정본(데이터) 단일 + 인터페이스 병존(나+다), v1 단원층만 은퇴 [HIGH]
**(2026-06-30 교정)** "단일화" 아님. 사용자 결정=나+다 혼합 병존:
- **데이터 정본 = 논점 frontmatter (단일)**. 인터페이스는 **board_server_v2(HTML, 데스크톱 입력) + 진도보드.base(Bases, 열람) 병존** — 둘 다 같은 frontmatter를 읽고 씀.
- 은퇴 대상 = **v1 단원층뿐**(board_server.py + 진도_state·진도_현황 단원집계 + _ingest_jindo). HTML 보드 자체(v2)는 유지.
- daily-drill SKILL `:148/:174/:177` v1 정본 단정(진도_board.html·진도_현황.json·_ingest_jindo) → **정본=논점 frontmatter**로 교체(인터페이스로 board_server_v2 HTML+Bases 병기, '단원'→'논점').
- CLAUDE.md `#19-B` progress.json 정본 → frontmatter 정본 명시, progress.json=보조 snapshot 격하.
- 런처 v1(`진도보드_서버켜기.bat`·`진도보드_백그라운드실행.bat`·`README.md`) → board_server_v2로 전환(autostart는 이미 v2).
- **v1 파일 격리(_retired)는 보류**: 파일 자체는 보존(역참조만 제거). 실제 _retired 이동은 **Bases 렌더 사용자 확인 게이트** 후(데이터·코드 손실 방지, #16-C 로그).

## B. 닫힌루프 키·경로 통일 [HIGH]
- 키 3중 분기: build_session(`과목\|중분류\|소단원`) / board_v2(`과목\|대분류\|논점`) / CLAUDE #50-A텍스트(`subj\|중분류\|단원`). → **`과목\|대분류\|논점`(v2 실구현)으로 통일**, #50-A·build_session·mark_progress 동기.
- `build_session.py` v1 죽은경로(WIKI=sync/위키/원문 폐기)+단원모델 → board_server_v2.build_board()처럼 **sync/위키/{과목} frontmatter에서 논점·회독·약점 읽도록 재작성**.
- `sync/위키/원문/` 죽은참조: CLAUDE.md #17표·#38·#46·#47(line118/141/287/306) + daily-drill:178 → **outputs/(원문)·sync/위키/{과목}(쟁점)** 로 교정. #17표 vs #38 직접충돌 최우선.

## C. 카드 룰·스킬 #51 정합 [HIGH]
- **마커 정의**: #51에 `카드감사`=누락탐지 작업 / `카드검증: 완료 (누락0)`=생성완료 마커 — 1줄 정의 추가(에이전트 `카드감사:완료` 오기 차단).
- **anki-card-generation 전면 재작성**: 죽은 입력(sync/wiki/쟁점)→sync/위키/{과목}, 카드 3역할(#51: A atom/풀이순서/사례전문), 태그축 `단계::`·`증명책임::`, 누락0 게이트 마커, 암기장 선참고.
- **case-answer-review**: 사례노트 `## 포섭격자`(요건×사실×증명책임) 연동 추가(현재 grep 0건).
- **daily-drill 출제**: #51 A카드(단계/증명책임)·풀이순서 카드·카드검증 게이트·사례노트 복습트랙 반영.
- 카드파일명 규약(_llamaparse_·_암기장_·_v37 3계열) #51/#46 명문화 + 빌더 정합(빌더는 이미 태그줄 흡수 수정됨).

## D. 과목 약칭 박멸 [HIGH]
- 활성코드: `problem-index/scripts/scan_problems.py:70` `return "민소법"`(+형소법) → 풀네임 + 누락과목(민사집행법·형총/형각·헌법·행정) 분류 보강. `socratic-core/docs/subject-detect.md` 약칭 표제.
- `_rename_과목.py:8` PAIRS old==new **no-op 버그**(자기치환됨) → 약칭 재정리에 재사용 불가. 수동/일회 교정으로 대체.
- 잔존 산출물 파일명(cardaudit/민소법_*·card_audit_민소법 등) = 이력성, 저우선.

## E. SRS·약점 단일화 + 죽은참조/은퇴 [MED]
- 약점 저장소 이원화(board_v2=learning.json / spaced-repetition SKILL=srs_log.json) → **learning.json 단일화** + srs_log 동기 규칙 + spaced-repetition에 mark_progress `--weak/--clear-weak` 연동 기술.
- korean-law-mcp 자동호출 잔존(law-note-supplement·study-notes·socratic-core·socratic-loader·legal-review) → law_api.py 재배선. `korean-law-mcp/SKILL.md:58` legal-socratic-rag(은퇴) 죽은참조 제거.
- `.claude/skills` 라우팅 스텁 결손(daily-drill·dispatch·weekly-review 등) → 미러 스텁 추가(#17/#48 진입점).
- CLAUDE.md #46 line301 stale(sync/위키/쟁점 이관완료), mark_progress.py 경로 미명시 → 보강.

## 우선순위 실행안
1. **A+B+C 마커정의**(HIGH, 회귀 직결): CLAUDE.md 교차참조 교정(원문경로·#19-20 v2·마커정의·키통일) + daily-drill/build_session v2 전환.
2. **C anki/case-answer**(HIGH): 카드 스킬 #51 재작성.
3. **D 약칭 활성코드**(HIGH): scan_problems 등.
4. **E**(MED): SRS단일화·죽은참조·라우팅스텁.
5. v1 은퇴·런처전환: 사용자 Bases 렌더 확인 게이트 후.
> 사례확장은 이 보완 후(특히 case-answer-review 포섭격자·daily-drill 사례트랙 정합돼야 사례노트가 닫힌루프에 물림).
