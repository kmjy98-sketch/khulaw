# 보완 — 런처·README·라우팅스텁 v2 (2026-06-30)

작업: `board_server.py → board_server_v2.py` 역참조 교체, README v2 갱신, `.claude/skills/` 결손 스텁 3종 추가.

---

## 변경 요약

### 1. 런처 .bat 역참조 교체

| 파일 | 변경 |
|------|------|
| `6.진도관리/진도보드_서버켜기.bat` | `board_server.py` → `board_server_v2.py` (7행) |
| `6.진도관리/진도보드_백그라운드실행.bat` | `board_server.py` → `board_server_v2.py` (7행) |

파일 자체(board_server.py)는 삭제·이동 없음 — 역참조만 교체(#16 준수).

### 2. 6.진도관리/README.md v2 갱신

- 진도 정본: 논점 frontmatter(`sync/위키/{과목}/{쟁점}.md`) 명시 (#19-B)
- 인터페이스 병존: `board_server_v2.py`(HTML) + `sync/위키/진도보드.base`(Bases)
- 역기록: `mark_progress.py` 명시
- `board_server_v2.py`의 `build_board` 동작(frontmatter 키 `과목|대분류|논점`, #50-A) 기술
- v1 자산 deprecated 표기: `board_server.py`·`진도_board.html`·`progress.json`·`진도_현황.json`·`진도_state.json`·`_ingest_jindo.py`
- 과목 풀네임 목록 추가 (#50-B, #51)
- v1 .bat·board_server.py 파일 자체는 유지(역참조만 변경)

### 3. .claude/skills/ 결손 스텁 3종 신설

| 스텁 경로 | 실체 경로 | description 트리거 |
|-----------|----------|-------------------|
| `.claude/skills/daily-drill/SKILL.md` | `.agent/skills/daily-drill/SKILL.md` | "오늘 공부/문제/드릴/복습", "/drill", "문제 내줘", "사례 채점", "올렸어" 등 |
| `.claude/skills/dispatch/SKILL.md` | `.agent/skills/dispatch/SKILL.md` | "이거 처리해줘", "/dispatch", "/do", 포괄 작업 |
| `.claude/skills/weekly-review/SKILL.md` | `.agent/skills/weekly-review/SKILL.md` | "주간리뷰", "이번주 정리", "weekly review" 등 |

형식: 기존 `.claude/skills/` 스텁과 동일 — description + `.agent/skills/{name}/SKILL.md` 읽기 지시 + 핵심 참조 1~2줄.

---

## 이슈

- `board_server.py`는 deprecated 표기만 함. Bases(`sync/위키/진도보드.base`) 렌더가 정상 확인된 후 `.agent/_retired/2026-06-30/`으로 이관 권고(#49). 이관 시 `log_file_op.py --execute`로 #16-C 로그 필수.
- `진도_board.html`은 v1 인터페이스. 서버모드 정착 후 동일 절차로 retired 대상.
- `daily-drill/SKILL.md` 내 "연동 스킬·파일" 섹션에 구 v1 경로(`진도_board.html`, `.agent/state/진도_현황.json`, `_ingest_jindo.py`) 잔존 — 해당 SKILL.md 자체는 `.agent/skills/daily-drill/SKILL.md` 원본이므로 별도 편집 요청 시 정리.
