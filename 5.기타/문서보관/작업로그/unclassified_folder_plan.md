# 미분류 폴더 처리 계획 (2026-03-15)

## 1) 대상 정의
- 기준: 루트 폴더 중 번호 체계(0,1,2,3,4,9) 또는 시스템 보호 폴더(`.agent`, `5.??/_inbox`, `5.??/_trash`, `5.??/_RAG_???`, `_????`, `_???`, `_hwp_??`)에 속하지 않는 폴더.
- 현재 대상:
  - `민사/` (파일 0개, legacy 잔여)
  - `hwp pdf 변환/` (파일 351개, 변환 작업 산출물)
  - `tmp/` (파일 9개, 임시 작업물)

## 2) 처리 원칙
- 원칙 A: 자동 분류 제외 폴더(`0.공유드라이브`, `9.스터디 답안지`)는 미분류 처리 대상에서 제외.
- 원칙 B: 빈 legacy 폴더는 삭제 대신 `5.??/_trash/{YYYY-MM-DD}/legacy_root/`로 이동.
- 원칙 C: 임시 폴더(`tmp`)는 보존기간 확인 후 `5.??/_trash/{YYYY-MM-DD}/tmp/`로 이동.
- 원칙 D: 변환 산출물(`hwp pdf 변환`)은 파일 유형별로
  - 교재/수업자료: 과목별 `??` ?? `??/{??_??}/??`
  - 시험/답안: 과목별 `보관/{교수_과목}/기출`
  - 분류불가: `5.??/_inbox/`? ??? ?? ??.

## 3) 실행 절차
1. 인벤토리 스냅샷 생성
   - 산출물: `.agent/state/unclassified_inventory_YYYY-MM-DD.json`
2. 규칙 매핑(파일명 키워드 + 확장자)
   - 산출물: `.agent/state/unclassified_move_plan_YYYY-MM-DD.json` (source/target/reason)
3. Dry-run 검토
   - 충돌/중복/경로오류 점검
4. 배치 실행(50개 단위)
   - 배치별 실행 후 검증 로그 저장
5. 사후 검증
   - 루트 미분류 폴더 재스캔, 남은 대상 재계획

## 4) 승인 필요 항목
- `hwp pdf 변환/`의 최종 귀속 정책
  - 옵션 1: 전량 `5.??/??/?????/` 보관
  - 옵션 2: 과목별 자동 분산(규칙 기반)
- `tmp/` 보존 기간 (즉시/7일/30일)

## 5) 완료 기준
- 루트 미분류 폴더 0개
- 예외 폴더는 번호/보호 규칙과 일치
- 실행 로그와 이동 계획 JSON이 `.agent/state/`에 저장
