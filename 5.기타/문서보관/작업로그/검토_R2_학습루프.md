---
type: 검토연구
주제: R2 학습루프 스킬 — 정본(#50/#51 위키3층·카드3역할·진도보드 v2) 대비 정합검토
일자: 2026-06-30
대상: daily-drill / case-answer-review / spaced-repetition (3개 스킬)
정본근거: CLAUDE.md #50(위키3층·닫힌루프)·#51(카드3역할·포섭격자) / sync/_meta/카드_사례_설계확정_2026-06-28.md / board_server_v2.py · sync/위키/진도보드.base · mark_progress.py
방식: 읽기전용. 근거=파일·라인. 추측 금지.
---

# R2 학습루프 스킬 정합검토

## 0. 검토 범위와 정본 기준선

정본(이 구조에 스킬이 정합해야 함):
- **위키 3층**(#50-B / 설계확정): 쟁점=`sync/위키/{과목}/{쟁점}.md` · 사례=`sync/위키/사례/{과목}/{사례}.md` · 원문=`outputs/01_ocr_llamaparse/`(sync 밖). 과목 풀네임(민사소송법·민사집행법, 약칭 금지).
- **카드 3역할**(#51): ①A 실질 atom(논점노트, `단계::`·`증명책임::`, 누락0 게이트 마커) ②풀이순서 카드(사례노트 `## 풀이순서`) ③사례 전문(카드화 비대상). 암기장 선참고.
- **진도보드 v2**(#50-A): 논점 frontmatter 정본. `6.진도관리/board_server_v2.py` + `sync/위키/진도보드.base`. 드릴 역기록=`mark_progress.py`.
- **닫힌루프 키 정합**(#50-A): "각 단계 산출은 다음 단계 입력 키로 정합." 단, 정본 텍스트는 키를 `subj|중분류|단원`으로 명시(아래 I1 충돌 참조).

검토 산출 5점검: ①진도보드 v1/v2 ②mark_progress 역기록 ③카드(#51)·사례노트(#50 사례층) 반영 ④과목 풀네임 ⑤카드검증 마커·약점 frontmatter 연동.

---

## 1. 실측 사실(파일·라인)

### 1.1 정본 v2 자산은 실재·동작
- `6.진도관리/board_server_v2.py` 존재. 정본=논점 frontmatter. 키 = `과목|대분류|논점제목(파일명)` (board_server_v2.py:69 `key = f"{s}|{dae}|{title}"`). SUBJ=과목 풀네임 7종(:25). type=="쟁점"만 수집(:67).
- `sync/위키/진도보드.base` 존재. `note.type == "쟁점"` 필터, properties=과목/대분류/논점/회독/선택/사례/기록/약점/최근복습(base:1-26).
- `mark_progress.py` 존재(daily-drill/scripts/). `sync/위키/{과목}/{제목}.md` frontmatter에 약점/회독/최근복습 기록(:30,38-43). `--notes "과목/제목"` 형식.
- 샘플 쟁점노트(`sync/위키/민법/가등기담보법에 의한 규율.md`) frontmatter: type:쟁점, 과목:민법, 회독/선택/사례/기록/약점/진도/최근복습/대분류 + `카드검증: 완료 (누락0, 2026-06-29)` + `카드감사: 완료` 마커 실재.
- 과목 풀네임 폴더 실재: 민법·민사소송법·민사집행법·행정법·헌법·형법각론·형법총론(sync/위키/).
- 사례노트층 실재(소수): `sync/위키/사례/민법/채권자취소권_…md`, `sync/위키/사례/형법총론/정당방위_…md`.

### 1.2 v1 자산도 잔존(은퇴 안 됨)
- `6.진도관리/진도_board.html`(v1, 37KB), `board_server.py`(v1, 22KB), `.agent/state/진도_현황.json`(v1 snapshot, 28KB), `.agent/scripts/_ingest_jindo.py`, `6.진도관리/data/진도_소단원_master.csv` 모두 존재. #49 은퇴 격리 안 됨.

### 1.3 daily-drill 핵심 스크립트(build_session.py)는 v1 전용
- `HTML = 6.진도관리/진도_board.html`(:22) — v1 HTML 가리킴.
- `HYUN = 진도_현황.json`(:27) — v1 snapshot.
- `MASTER = 진도_소단원_master.csv`(:23) — 단원 매칭 마스터.
- `WIKI = sync/위키/원문`(:32) — **이 폴더는 존재하지 않음(#50으로 폐기, 실측 NOT FOUND)**. 죽은 참조.
- 키 형식 = `과목|중분류|소단원`(:468,490,515,529 `f"{subj}|{mid}|{u}"`). 보드 v2 키(`과목|대분류|논점`)와 세그먼트 명칭·구성 모두 불일치.
- 전체 로직이 '보드단원↔마스터 소단원' 토큰매칭(:125-241)에 묶임 = **단원 모델**. 논점 frontmatter를 읽지 않음.

---

## 2. 점검결과 (5축)

### ① 진도보드 v1↔v2 (단원↔논점 불일치) — 핵심 결함
- **daily-drill 본문은 v1.** 전반(description·Phase 0~2·연동파일)이 "단원" 모델. SKILL.md:148 "회독 카운트는 **보드(`진도_board.html`)가 정본**", :174 "보드 정본은 …`_ingest_jindo.py`로 반영된 `진도_현황.json`", :177 연동파일=`6.진도관리/진도_board.html`·`진도_현황.json`·`_ingest_jindo.py` — **전부 v1**.
- **부분적으로만 v2.** Phase 3의 역기록 블록(:149-154)만 "Bases 논점 역기록…`sync/위키/진도보드.base` 정본"으로 v2. → 한 스킬 안에 v1(정본 주장)·v2(정본 주장)가 **이중 정본**으로 공존. :154 스스로 "단원↔논점 회독 통합 매핑은 미정 설계"라 인정.
- **키 3중 분기**: build_session=`과목|중분류|소단원` / board_v2·base·export=`과목|대분류|논점` / CLAUDE.md#50-A 텍스트=`subj|중분류|단원`. 세 곳이 다 다름 → 닫힌루프 키정합(#50-A) 미충족.
- **죽은참조**: build_session.py:32 `sync/위키/원문` 폴더 부존재. SKILL.md:178도 `sync/위키/원문/{책}/` 인용(폐기 경로).

### ② mark_progress.py 역기록 연동
- 스크립트 실재·정상. SKILL.md:149-154에 호출법 반영됨(정합 OK).
- 단, build_session(입력) 키와 mark_progress(출력) 키가 다른 모델 → 같은 논점을 가리켜도 키가 안 맞아 **드릴 선정→역기록 자동 왕복이 폐곡선이 안 됨**(에이전트 수동 명시로만 메움). #50-A 닫힌루프 단선.

### ③ #51 카드·#50 사례노트 반영
- **daily-drill 출제소스가 구 카드 모델.** SKILL.md:62·178이 `outputs/02_cards_v37/*.md` 의 `rule/cloze/case_law`만 사용. #51의 `단계::`·`증명책임::`·풀이순서 카드·A/사례 3역할 미반영. build_session도 02_cards_v37 단원매칭만(:259-).
- **사례노트층(`sync/위키/사례/{과목}/`) 미인지.** daily-drill 사례 출제는 `case_problem_answer_index.json`(책 OCR span) 경로(:108)만. 신설 사례노트(문제/풀이순서/포섭격자/모범답안 5섹션)를 출제·채점 소스로 쓰지 않음.
- **case-answer-review에 포섭격자/사례노트 0건.** grep 결과 `사례노트`·`포섭격자`·`sync/위키/사례`·`claim-chart`·`증명책임`·`풀이순서` 모두 미참조. 설계확정(:31,45)은 사례 채점의 사실↔요건을 `## 포섭격자`(claim-chart)로 본다고 확정했으나 채점 스킬은 구 루브릭(쟁점0.25…)·자체 포섭 4슬롯만 보유 — 포섭격자 셀 연동 없음.
- 모범답안 미리노출 금지는 양쪽(daily-drill:108, 설계:33)이 같은 취지로 정합.

### ④ 과목 풀네임
- 3개 스킬에 약칭(민소법/민집법/형소/형총/형각) 사용 0건(정합).
- 단 daily-drill 예시는 민법/형법만 등장, 풀네임 7과목 명시는 board_server_v2(:25)에만. 스킬 자체엔 풀네임 강제 문구 없음(경미).

### ⑤ 카드검증 마커·약점 frontmatter 연동
- 카드검증 마커(`카드검증: 완료 (누락0)`)는 쟁점노트에 실재(1.1). 그러나 **daily-drill·case-answer-review 어디도 이 마커를 출제 게이트로 참조 안 함**. #51은 "누락0 게이트"를 카드 정합 조건으로 두는데, 드릴이 이를 안 보고 02_cards_v37를 직접 읽어 출제 → 마커가 루프에 안 물림.
- 약점 frontmatter(`약점: true/false`)는 board_v2(:73)·mark_progress(:38)·base(약점 view)가 쓰고 읽음. 단 spaced-repetition SKILL은 `약점`·`frontmatter`·`learning.json`·`mark_progress`·`board_server` 전부 미참조(grep 0건) — SRS는 `srs_log.json`만, 보드 약점층과 단절. board_server_v2는 약점/SRS를 `learning.json`에서 읽음(:155-164)인데 spaced-repetition 스킬 문서는 `srs_log.json` 저장(:94)이라 **약점 저장소 이원화(learning.json vs srs_log.json) 미정리**.

---

## 3. 결론(요지)
- 정본 v2(논점 frontmatter·base·mark_progress·카드/사례 마커)는 **데이터·서버층에선 구축됨**.
- **스킬 문서·핵심 스크립트(build_session.py)는 여전히 v1 단원 모델**이라 루프가 두 모델로 쪼개져 있음(이중 정본·키 3중 분기·죽은 경로 `sync/위키/원문`).
- #51 카드 3역할·#50 사례노트·포섭격자가 드릴/채점 스킬에 미반영.
- 회귀 근본원인(메모리: 지시 미영속화)과 동형 — 설계확정(2026-06-28)·#50/#51 룰 신설이 **스킬 SKILL.md/스크립트로 영속화되지 않음**.
